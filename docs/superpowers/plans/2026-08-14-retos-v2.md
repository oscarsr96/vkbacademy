# Retos v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rediseñar el catálogo de retos para que mida el flujo real del alumno (plan de estudio generado por IA), añadir misiones semanales y persistir por primera vez los ejercicios resueltos.

**Architecture:** Se extiende el motor de gamificación existente en vez de sustituirlo. `Challenge` gana una cadencia (`PERMANENT` | `WEEKLY`), `UserChallenge` gana un `periodKey` que convierte el unique en triple, y `calculateProgress` acepta una ventana temporal opcional. Se añade el modelo `ExerciseAttempt` (con corrección en servidor) y dos rachas nuevas denormalizadas en `User`.

**Tech Stack:** NestJS + Prisma + PostgreSQL (API), React 18 + Vite + React Query (web), Jest (tests). Monorepo pnpm/Turborepo.

**Spec:** [`docs/superpowers/specs/2026-08-14-retos-v2-design.md`](../specs/2026-08-14-retos-v2-design.md)

## Global Constraints

- **TypeScript `strict: true`.** Sin `any` salvo justificación escrita en comentario.
- **Nombres en inglés** (variables, funciones, clases, rutas, columnas). **Comentarios en español.**
- **`checkAndAward` siempre con `void`**, nunca `await`: no debe bloquear la respuesta HTTP.
- **Guards antes de services**: la lógica de roles va en guard/decorador, nunca en el service.
- **Errores**: `HttpException` de NestJS con mensaje descriptivo en español.
- **DTOs con `class-validator`** en todos los endpoints.
- **Filtro pnpm con scope**: `pnpm --filter @vkbacademy/api`, nunca `--filter api`.
- **Nunca `--no-verify`** ni skip de hooks de git.
- **Estilo de commit**: `feat(challenges):`, `fix(challenges):`, `refactor:`, `chore:`. Seguir el `git log` existente.
- **Contenido interactivo vive en JSON** (`StudyPlan.exercises`): no crear modelos nuevos para los ejercicios en sí.
- Los valores de `verdict` son exactamente `'correct' | 'partial' | 'incorrect'`.
- Los valores de `difficulty` son exactamente `'EASY' | 'MEDIUM' | 'HARD'`.
- Los valores de `AiExamBank.level` son exactamente `'BASIC' | 'MEDIUM' | 'HARD'`.
- El día del alumno se calcula siempre en **`Europe/Madrid`**, nunca en UTC.

---

## File Structure

**API — `apps/api/`**

| Fichero | Responsabilidad |
| ------- | --------------- |
| `prisma/schema.prisma` | Enum `ChallengeCadence`, 10 valores nuevos y 5 fuera de `ChallengeType`, `Challenge.cadence`, `UserChallenge.periodKey`, modelo `ExerciseAttempt`, 4 campos de racha en `User` |
| `prisma/migrations/<ts>_retos_v2/migration.sql` | SQL reordenado a mano (borrado de filas antes del `AlterEnum`) |
| `prisma/seed.ts` | Catálogo nuevo de retos cubriendo los 14 tipos |
| `src/challenges/challenge-periods.ts` | **Nuevo.** Helpers puros de fecha: semana ISO, día en Madrid, inicio de semana, `WEEKLY_CAPABLE_TYPES` |
| `src/challenges/challenges.service.ts` | `updateStreak` (dos rachas), `calculateProgress` (14 tipos + ventana), `checkAndAward` (periodo + streaks), `bumpCorrectStreak`, `getMyProgress`, `getSummary` |
| `src/exercises/exercises.service.ts` | Inyecta `id` a cada ejercicio generado; expone `normalizeForMatch` |
| `src/study-plans/study-plans.service.ts` | `withExerciseIds`, `submitExerciseAttempt`, llamada a `checkAndAward` en `create` |
| `src/study-plans/dto/submit-exercise-attempt.dto.ts` | **Nuevo.** `{ answer: string }` |
| `src/study-plans/study-plans.controller.ts` | Ruta `POST /study-plans/:id/exercises/:exerciseId/attempt` |
| `src/tutor/tutor.service.ts` + `tutor.module.ts` | Llamada a `checkAndAward(TUTOR_QUESTIONS)` |
| `src/exams/exams.service.ts`, `src/theory/theory.service.ts`, `src/progress/progress.service.ts`, `src/quizzes/quizzes.service.ts` | Tipos de evento actualizados |
| `src/admin/dto/create-challenge.dto.ts`, `update-challenge.dto.ts` | Campo `cadence` |
| `src/admin/admin-gamification.service.ts` | Validación cadencia × tipo |

**Shared — `packages/shared/src/types/study.types.ts`**: campo `id` en `StudyExercise`.

**Web — `apps/web/src/`**: `api/challenges.api.ts`, `api/admin.api.ts`, `pages/admin/AdminChallengesPage.tsx`, `pages/ChallengesPage.tsx`, `components/exercises/ExercisePractice.tsx`.

**Orden de dependencias:** Task 1 (schema) desbloquea todo. Task 6 (ids de ejercicio) debe ir antes de Task 7 (endpoint de intento). Las tareas de web (11-13) dependen de la API correspondiente.

---

## Task 1: Schema y migración

**Files:**
- Modify: `apps/api/prisma/schema.prisma`
- Create: `apps/api/prisma/migrations/<timestamp>_retos_v2/migration.sql` (generado y luego editado)

**Interfaces:**
- Consumes: nada.
- Produces: enum `ChallengeCadence { PERMANENT, WEEKLY }`; `ChallengeType` con los valores `STUDY_PLAN_CREATED`, `TOPICS_STUDIED`, `SUBJECT_VARIETY`, `EXERCISES_SOLVED`, `EXERCISES_CORRECT_STREAK`, `HARD_EXERCISES_SOLVED`, `EXAM_PERFECT`, `EXAM_HARD_SCORE`, `TUTOR_QUESTIONS`, `STREAK_DAILY`, `THEORY_COMPLETED`, `EXAM_COMPLETED`, `EXAM_SCORE`, `STREAK_WEEKLY`; modelo `ExerciseAttempt`; campos `User.lastActiveDay`, `User.currentDailyStreak`, `User.longestDailyStreak`, `User.currentCorrectStreak`, `User.longestCorrectStreak`; `Challenge.cadence`; `UserChallenge.periodKey`.

- [ ] **Step 1: Reemplazar el enum `ChallengeType` en `schema.prisma`**

Sustituir el bloque `enum ChallengeType { ... }` completo (líneas ~317-327) por:

```prisma
enum ChallengeCadence {
  PERMANENT // logro acumulado de por vida
  WEEKLY // misión que se reinicia cada semana ISO
}

enum ChallengeType {
  // ── Plan de estudio ──
  STUDY_PLAN_CREATED // crea N planes de estudio
  TOPICS_STUDIED // estudia N temas (StudyPlanTopic)
  SUBJECT_VARIETY // toca N asignaturas distintas (contextCourseId)
  THEORY_COMPLETED // genera N mazos de teoría (TheoryModule)

  // ── Ejercicios del plan ──
  EXERCISES_SOLVED // acierta N ejercicios
  HARD_EXERCISES_SOLVED // acierta N ejercicios de dificultad HARD
  EXERCISES_CORRECT_STREAK // N aciertos seguidos sin fallar

  // ── Exámenes ──
  EXAM_COMPLETED // entrega N exámenes
  EXAM_SCORE // consigue ≥ N% en cualquier examen
  EXAM_PERFECT // consigue N exámenes con 100%
  EXAM_HARD_SCORE // consigue ≥ N% en un examen de nivel HARD

  // ── Hábito ──
  TUTOR_QUESTIONS // hace N preguntas al tutor IA
  STREAK_DAILY // racha de N días consecutivos
  STREAK_WEEKLY // racha de N semanas consecutivas
}
```

- [ ] **Step 2: Añadir `cadence` a `Challenge` y `periodKey` a `UserChallenge`**

En `model Challenge`, después de `type ChallengeType`:

```prisma
  cadence     ChallengeCadence @default(PERMANENT)
```

En `model UserChallenge`, sustituir el bloque de índices al final del modelo:

```prisma
  periodKey String @default("ALL") // "ALL" en PERMANENT | "2026-W33" en WEEKLY

  @@unique([userId, challengeId, periodKey])
  @@index([userId])
  @@index([userId, periodKey])
  @@index([academyId])
```

- [ ] **Step 3: Añadir el modelo `ExerciseAttempt`**

Justo después de `model UserChallenge`:

```prisma
// Intento de un ejercicio del plan de estudio. Un ejercicio vive en
// StudyPlan.exercises (Json) y se identifica por su `id` estable; esta
// tabla solo guarda el resultado, corregido siempre en servidor.
model ExerciseAttempt {
  id          String   @id @default(cuid())
  userId      String
  studyPlanId String
  exerciseId  String // id estable del item dentro de StudyPlan.exercises
  topicLabel  String
  difficulty  String // EASY | MEDIUM | HARD
  verdict     String // correct | partial | incorrect
  answeredAt  DateTime @default(now())

  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  studyPlan StudyPlan @relation(fields: [studyPlanId], references: [id], onDelete: Cascade)

  // Un intento por ejercicio: reintentar actualiza la fila, no crea otra.
  @@unique([userId, exerciseId])
  @@index([userId, answeredAt])
}
```

- [ ] **Step 4: Añadir los campos de racha y las relaciones inversas**

En `model User`, junto a `currentStreak` / `longestStreak` / `lastActiveWeek`:

```prisma
  lastActiveDay        String? // "2026-08-14" calculado en Europe/Madrid
  currentDailyStreak   Int     @default(0)
  longestDailyStreak   Int     @default(0)
  currentCorrectStreak Int     @default(0) // aciertos seguidos en ejercicios
  longestCorrectStreak Int     @default(0)
```

Y en la lista de relaciones de `User`:

```prisma
  exerciseAttempts ExerciseAttempt[]
```

En `model StudyPlan`, en su lista de relaciones:

```prisma
  exerciseAttempts ExerciseAttempt[]
```

- [ ] **Step 5: Generar la migración SIN aplicarla**

```bash
cd apps/api && pnpm prisma migrate dev --name retos_v2 --create-only
```

Esto crea `prisma/migrations/<timestamp>_retos_v2/migration.sql` pero no lo ejecuta.

- [ ] **Step 6: Reordenar el SQL a mano**

Abrir el `migration.sql` generado. Prisma pondrá el `AlterEnum` de `ChallengeType` sin borrar antes las filas que usan los valores retirados, y **Postgres fallará**: no se puede eliminar un valor de un enum mientras haya filas que lo usen.

Insertar al principio del fichero, antes de cualquier `AlterEnum`:

```sql
-- Retos v2: los tipos EXERCISE_COMPLETED, EXERCISE_SCORE y TOTAL_HOURS_*
-- desaparecen del enum. Postgres no permite quitar un valor mientras haya
-- filas que lo usen, así que se borran primero los retos de esos tipos y
-- su progreso. Los puntos ya concedidos se quedan en User.totalPoints:
-- no hay clawback (decisión D9 del diseño).
DELETE FROM "UserChallenge"
WHERE "challengeId" IN (
  SELECT "id" FROM "Challenge"
  WHERE "type" IN (
    'EXERCISE_COMPLETED', 'EXERCISE_SCORE',
    'TOTAL_HOURS_EXERCISE', 'TOTAL_HOURS_THEORY', 'TOTAL_HOURS_EXAM'
  )
);

DELETE FROM "Challenge"
WHERE "type" IN (
  'EXERCISE_COMPLETED', 'EXERCISE_SCORE',
  'TOTAL_HOURS_EXERCISE', 'TOTAL_HOURS_THEORY', 'TOTAL_HOURS_EXAM'
);
```

Comprobar además que el `DROP INDEX` del unique viejo de `UserChallenge` va **antes** del `CREATE UNIQUE INDEX` del triple, y que la columna `periodKey` se crea con su `DEFAULT 'ALL'` antes de ese índice.

**Importante:** el checksum de la migración se congela en el primer `migrate dev`. Si se aplica sin revisar y luego se edita, hay que borrar la carpeta de la migración y rehacerla.

- [ ] **Step 7: Aplicar la migración**

```bash
docker compose up -d
cd apps/api && pnpm prisma migrate dev
```

Expected: la migración se aplica sin errores y el cliente Prisma se regenera.

- [ ] **Step 8: Verificar que el cliente Prisma tiene los tipos nuevos**

```bash
pnpm --filter @vkbacademy/api exec tsc --noEmit
```

Expected: FALLA, con errores en `challenges.service.ts` (casos `EXERCISE_COMPLETED`, `EXERCISE_SCORE`, `TOTAL_HOURS_*` que ya no existen en el enum) y en `progress.service.ts`, `theory.service.ts`, `exams.service.ts`, `quizzes.service.ts`. **Esto es lo esperado**: las tareas 2-8 los arreglan. Anotar la lista de errores.

- [ ] **Step 9: Commit**

```bash
git add apps/api/prisma/schema.prisma apps/api/prisma/migrations
git commit -m "feat(challenges): schema de retos v2 con cadencia, periodo e intentos de ejercicio"
```

---

## Task 2: Helpers de periodo y racha diaria

**Files:**
- Create: `apps/api/src/challenges/challenge-periods.ts`
- Create: `apps/api/src/challenges/challenge-periods.spec.ts`

**Interfaces:**
- Consumes: `ChallengeType` de `@prisma/client` (Task 1).
- Produces:
  - `isoWeek(date: Date): string` — `"2026-W08"`
  - `previousIsoWeek(week: string): string`
  - `currentWeekStart(now: Date): Date` — lunes 00:00 UTC de la semana ISO de `now`
  - `madridDay(date: Date): string` — `"2026-08-14"` en `Europe/Madrid`
  - `previousDay(day: string): string`
  - `WEEKLY_CAPABLE_TYPES: ChallengeType[]`
  - `isWeeklyCapable(type: ChallengeType): boolean`

- [ ] **Step 1: Escribir el test que falla**

Crear `apps/api/src/challenges/challenge-periods.spec.ts`:

```ts
import { ChallengeType } from '@prisma/client';
import {
  isoWeek,
  previousIsoWeek,
  currentWeekStart,
  madridDay,
  previousDay,
  isWeeklyCapable,
} from './challenge-periods';

describe('challenge-periods', () => {
  describe('isoWeek', () => {
    it('devuelve la semana ISO de un lunes', () => {
      expect(isoWeek(new Date('2026-02-16T12:00:00Z'))).toBe('2026-W08');
    });

    it('devuelve la misma semana para el domingo siguiente', () => {
      expect(isoWeek(new Date('2026-02-22T23:00:00Z'))).toBe('2026-W08');
    });
  });

  describe('previousIsoWeek', () => {
    it('retrocede una semana dentro del mismo año', () => {
      expect(previousIsoWeek('2026-W08')).toBe('2026-W07');
    });

    it('cruza el cambio de año hacia la última semana del año anterior', () => {
      expect(previousIsoWeek('2026-W01')).toBe('2025-W52');
    });
  });

  describe('currentWeekStart', () => {
    it('devuelve el lunes 00:00 UTC de la semana en curso', () => {
      expect(currentWeekStart(new Date('2026-02-19T17:45:00Z')).toISOString()).toBe(
        '2026-02-16T00:00:00.000Z',
      );
    });

    it('devuelve el mismo lunes cuando ya es lunes', () => {
      expect(currentWeekStart(new Date('2026-02-16T00:30:00Z')).toISOString()).toBe(
        '2026-02-16T00:00:00.000Z',
      );
    });
  });

  describe('madridDay', () => {
    // Verano: Madrid es UTC+2. Las 23:30 UTC ya son el día siguiente en Madrid.
    it('usa el huso de Madrid, no UTC, en horario de verano', () => {
      expect(madridDay(new Date('2026-08-14T23:30:00Z'))).toBe('2026-08-15');
    });

    // Invierno: Madrid es UTC+1. Las 00:30 de Madrid son las 23:30 UTC del día anterior.
    it('usa el huso de Madrid, no UTC, en horario de invierno', () => {
      expect(madridDay(new Date('2026-01-14T23:30:00Z'))).toBe('2026-01-15');
    });
  });

  describe('previousDay', () => {
    it('retrocede un día', () => {
      expect(previousDay('2026-08-14')).toBe('2026-08-13');
    });

    it('cruza el cambio de mes', () => {
      expect(previousDay('2026-03-01')).toBe('2026-02-28');
    });
  });

  describe('isWeeklyCapable', () => {
    it('acepta los tipos contables en ventana', () => {
      expect(isWeeklyCapable(ChallengeType.EXERCISES_SOLVED)).toBe(true);
      expect(isWeeklyCapable(ChallengeType.TOPICS_STUDIED)).toBe(true);
      expect(isWeeklyCapable(ChallengeType.EXAM_COMPLETED)).toBe(true);
    });

    it('rechaza los tipos de estado', () => {
      expect(isWeeklyCapable(ChallengeType.STREAK_DAILY)).toBe(false);
      expect(isWeeklyCapable(ChallengeType.EXAM_SCORE)).toBe(false);
      expect(isWeeklyCapable(ChallengeType.SUBJECT_VARIETY)).toBe(false);
      expect(isWeeklyCapable(ChallengeType.EXERCISES_CORRECT_STREAK)).toBe(false);
    });
  });
});
```

- [ ] **Step 2: Ejecutar el test y comprobar que falla**

```bash
pnpm --filter @vkbacademy/api test -- challenge-periods
```

Expected: FAIL — `Cannot find module './challenge-periods'`.

- [ ] **Step 3: Escribir la implementación**

Crear `apps/api/src/challenges/challenge-periods.ts`:

```ts
import { ChallengeType } from '@prisma/client';

/** Devuelve la semana ISO como "2026-W07" */
export function isoWeek(date: Date): string {
  const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
  // Ajustar al jueves de la semana actual (ISO: la semana empieza el lunes)
  d.setUTCDate(d.getUTCDate() + 4 - (d.getUTCDay() || 7));
  const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
  const week = Math.ceil(((d.getTime() - yearStart.getTime()) / 86_400_000 + 1) / 7);
  return `${d.getUTCFullYear()}-W${String(week).padStart(2, '0')}`;
}

/** Devuelve la semana ISO anterior a la dada */
export function previousIsoWeek(week: string): string {
  const [yearStr, wStr] = week.split('-W');
  const year = parseInt(yearStr, 10);
  const w = parseInt(wStr, 10);
  if (w === 1) {
    // Semana 1 del año: la anterior es la última del año previo
    const dec28 = new Date(Date.UTC(year - 1, 11, 28));
    return isoWeek(dec28);
  }
  // Calcular lunes de la semana anterior
  const jan4 = new Date(Date.UTC(year, 0, 4));
  jan4.setUTCDate(jan4.getUTCDate() - (jan4.getUTCDay() || 7) + 1);
  jan4.setUTCDate(jan4.getUTCDate() + (w - 2) * 7);
  return isoWeek(jan4);
}

/** Lunes 00:00 UTC de la semana ISO en curso. Es el `since` de los retos WEEKLY. */
export function currentWeekStart(now: Date): Date {
  const d = new Date(Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate()));
  d.setUTCDate(d.getUTCDate() - ((d.getUTCDay() || 7) - 1));
  return d;
}

/**
 * Día del alumno como "2026-08-14", en Europe/Madrid.
 * En UTC, estudiar a la 01:00 de Madrid contaría como el día anterior y
 * rompería la racha de forma invisible. `en-CA` da el formato ISO.
 */
export function madridDay(date: Date): string {
  return date.toLocaleDateString('en-CA', { timeZone: 'Europe/Madrid' });
}

/** Día anterior a "2026-08-14". Se ancla a mediodía UTC para no depender del DST. */
export function previousDay(day: string): string {
  const d = new Date(`${day}T12:00:00Z`);
  d.setUTCDate(d.getUTCDate() - 1);
  return d.toISOString().slice(0, 10);
}

/**
 * Tipos que pueden configurarse con cadencia WEEKLY: los contables dentro de
 * una ventana temporal. Los de estado (máximos, rachas, variedad acumulada)
 * no tienen sentido reiniciados cada semana.
 */
export const WEEKLY_CAPABLE_TYPES: ChallengeType[] = [
  ChallengeType.STUDY_PLAN_CREATED,
  ChallengeType.TOPICS_STUDIED,
  ChallengeType.THEORY_COMPLETED,
  ChallengeType.EXERCISES_SOLVED,
  ChallengeType.HARD_EXERCISES_SOLVED,
  ChallengeType.EXAM_COMPLETED,
  ChallengeType.EXAM_PERFECT,
  ChallengeType.TUTOR_QUESTIONS,
];

export function isWeeklyCapable(type: ChallengeType): boolean {
  return WEEKLY_CAPABLE_TYPES.includes(type);
}
```

- [ ] **Step 4: Ejecutar el test y comprobar que pasa**

```bash
pnpm --filter @vkbacademy/api test -- challenge-periods
```

Expected: PASS, 12 tests.

- [ ] **Step 5: Verificación por mutación**

Cambiar `timeZone: 'Europe/Madrid'` por `timeZone: 'UTC'` en `madridDay` y volver a ejecutar. Expected: FALLAN los dos tests de `madridDay`. Deshacer el cambio y confirmar que vuelven a pasar.

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/challenges/challenge-periods.ts apps/api/src/challenges/challenge-periods.spec.ts
git commit -m "feat(challenges): helpers de periodo, dia en Madrid y tipos con cadencia semanal"
```

---

## Task 3: `updateStreak` mantiene racha semanal y diaria

**Files:**
- Modify: `apps/api/src/challenges/challenges.service.ts`
- Test: `apps/api/src/challenges/challenges.service.spec.ts`

**Interfaces:**
- Consumes: `isoWeek`, `previousIsoWeek`, `madridDay`, `previousDay` de `./challenge-periods` (Task 2).
- Produces: `ChallengesService.updateStreak(userId: string): Promise<void>` — actualiza en una sola pasada `lastActiveWeek`/`currentStreak`/`longestStreak` y `lastActiveDay`/`currentDailyStreak`/`longestDailyStreak`.

- [ ] **Step 1: Escribir los tests que fallan**

En `challenges.service.spec.ts`, dentro del `describe('updateStreak')`, añadir estos tests. La fecha fija `WEEK_08` (`2026-02-16T12:00:00Z`) es un lunes; en Madrid ese instante es el **2026-02-16**.

```ts
const DAY_W08_MON = '2026-02-16'; // el día de WEEK_08 en Europe/Madrid
const DAY_W08_SUN = '2026-02-15'; // el día anterior

it('inicia la racha diaria a 1 cuando no hay dia previo', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 0,
    longestStreak: 0,
    lastActiveWeek: null,
    currentDailyStreak: 0,
    longestDailyStreak: 0,
    lastActiveDay: null,
  });
  mockPrisma.user.update.mockResolvedValue({});

  await service.updateStreak('user1');

  expect(mockPrisma.user.update).toHaveBeenCalledWith({
    where: { id: 'user1' },
    data: {
      lastActiveWeek: ISO_W08,
      currentStreak: 1,
      longestStreak: 1,
      lastActiveDay: DAY_W08_MON,
      currentDailyStreak: 1,
      longestDailyStreak: 1,
    },
  });
});

it('incrementa la racha diaria si el dia anterior fue el ultimo activo', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 2,
    longestStreak: 4,
    lastActiveWeek: ISO_W08, // misma semana: la racha semanal no se toca
    currentDailyStreak: 3,
    longestDailyStreak: 3,
    lastActiveDay: DAY_W08_SUN,
  });
  mockPrisma.user.update.mockResolvedValue({});

  await service.updateStreak('user1');

  expect(mockPrisma.user.update).toHaveBeenCalledWith({
    where: { id: 'user1' },
    data: {
      lastActiveDay: DAY_W08_MON,
      currentDailyStreak: 4,
      longestDailyStreak: 4,
    },
  });
});

it('reinicia la racha diaria a 1 si se salto un dia', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 2,
    longestStreak: 4,
    lastActiveWeek: ISO_W08,
    currentDailyStreak: 6,
    longestDailyStreak: 9,
    lastActiveDay: '2026-02-13', // saltó el 14 y el 15
  });
  mockPrisma.user.update.mockResolvedValue({});

  await service.updateStreak('user1');

  expect(mockPrisma.user.update).toHaveBeenCalledWith({
    where: { id: 'user1' },
    data: {
      lastActiveDay: DAY_W08_MON,
      currentDailyStreak: 1,
      longestDailyStreak: 9, // el récord no baja
    },
  });
});

it('no escribe nada si ya hubo actividad hoy y esta semana', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 2,
    longestStreak: 4,
    lastActiveWeek: ISO_W08,
    currentDailyStreak: 3,
    longestDailyStreak: 5,
    lastActiveDay: DAY_W08_MON,
  });

  await service.updateStreak('user1');

  expect(mockPrisma.user.update).not.toHaveBeenCalled();
});
```

Actualizar también los tests existentes de `updateStreak`: sus `mockResolvedValue` deben incluir los tres campos nuevos (`currentDailyStreak: 0`, `longestDailyStreak: 0`, `lastActiveDay: null`), y sus asserts deben esperar además `lastActiveDay: DAY_W08_MON`, `currentDailyStreak: 1`, `longestDailyStreak: 1`.

- [ ] **Step 2: Ejecutar los tests y comprobar que fallan**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: FAIL — el `data` del `user.update` no incluye los campos diarios.

- [ ] **Step 3: Reescribir `updateStreak`**

En `challenges.service.ts`, borrar las funciones locales `isoWeek` y `previousIsoWeek` del principio del fichero (ahora viven en `challenge-periods.ts`) y añadir el import:

```ts
import { Prisma } from '@prisma/client';
import { isoWeek, previousIsoWeek, madridDay, previousDay } from './challenge-periods';
```

`currentWeekStart` se importa en la Task 5, cuando `checkAndAward` lo necesite. Añadirlo ahora deja un import sin usar y el lint lo rechaza.

Sustituir el método `updateStreak` completo por:

```ts
  /** Actualiza las rachas semanal y diaria del usuario en una sola escritura */
  async updateStreak(userId: string): Promise<void> {
    const user = await this.prisma.user.findUnique({
      where: { id: userId },
      select: {
        currentStreak: true,
        longestStreak: true,
        lastActiveWeek: true,
        currentDailyStreak: true,
        longestDailyStreak: true,
        lastActiveDay: true,
      },
    });
    if (!user) return;

    const now = new Date();
    const currentWeek = isoWeek(now);
    const currentDay = madridDay(now);

    const weekChanged = user.lastActiveWeek !== currentWeek;
    const dayChanged = user.lastActiveDay !== currentDay;
    // Ya se contabilizó este día y esta semana: nada que escribir
    if (!weekChanged && !dayChanged) return;

    const data: Prisma.UserUpdateInput = {};

    if (weekChanged) {
      const nextWeekly =
        user.lastActiveWeek === previousIsoWeek(currentWeek) ? user.currentStreak + 1 : 1;
      data.lastActiveWeek = currentWeek;
      data.currentStreak = nextWeekly;
      data.longestStreak = Math.max(user.longestStreak, nextWeekly);
    }

    if (dayChanged) {
      const nextDaily =
        user.lastActiveDay === previousDay(currentDay) ? user.currentDailyStreak + 1 : 1;
      data.lastActiveDay = currentDay;
      data.currentDailyStreak = nextDaily;
      data.longestDailyStreak = Math.max(user.longestDailyStreak, nextDaily);
    }

    await this.prisma.user.update({ where: { id: userId }, data });
  }
```

- [ ] **Step 4: Ejecutar los tests y comprobar que pasan**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: PASS en todo el bloque `updateStreak`. Otros bloques del fichero pueden seguir rojos por el enum (se arreglan en Task 4).

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/challenges/challenges.service.ts apps/api/src/challenges/challenges.service.spec.ts
git commit -m "feat(challenges): updateStreak mantiene racha diaria en Europe/Madrid"
```

---

## Task 4: `calculateProgress` con los 14 tipos y ventana temporal

**Files:**
- Modify: `apps/api/src/challenges/challenges.service.ts`
- Test: `apps/api/src/challenges/challenges.service.spec.ts`

**Interfaces:**
- Consumes: helpers de Task 2, schema de Task 1.
- Produces: `private calculateProgress(userId: string, type: ChallengeType, since?: Date): Promise<number>` — con `since`, los tipos contables filtran por fecha; los de estado lo ignoran.

- [ ] **Step 1: Escribir los tests que fallan**

En `challenges.service.spec.ts`, ampliar primero el `mockPrisma` del `beforeEach` con los modelos nuevos:

```ts
    studyPlan: { count: jest.fn() },
    studyPlanTopic: { count: jest.fn(), findMany: jest.fn() },
    exerciseAttempt: { count: jest.fn(), findUnique: jest.fn(), create: jest.fn(), update: jest.fn() },
    tutorMessage: { count: jest.fn() },
```

Y añadir sus declaraciones al tipo de `mockPrisma`:

```ts
    studyPlan: { count: jest.Mock };
    studyPlanTopic: { count: jest.Mock; findMany: jest.Mock };
    exerciseAttempt: {
      count: jest.Mock;
      findUnique: jest.Mock;
      create: jest.Mock;
      update: jest.Mock;
    };
    tutorMessage: { count: jest.Mock };
```

Después añadir este `describe` nuevo al final del fichero. `calculateProgress` es privado, así que se ejerce a través de `checkAndAward`, que es la vía real:

```ts
describe('calculateProgress vía checkAndAward', () => {
  /** Monta un reto y devuelve el progreso que se escribió en el upsert */
  async function progressFor(
    challenge: { type: ChallengeType; cadence?: ChallengeCadence; target?: number },
  ): Promise<number> {
    mockPrisma.user.findUnique.mockResolvedValue({
      currentStreak: 1,
      longestStreak: 1,
      lastActiveWeek: ISO_W08,
      currentDailyStreak: 1,
      longestDailyStreak: 1,
      lastActiveDay: '2026-02-16',
      currentCorrectStreak: 7,
    });
    mockPrisma.challenge.findMany.mockResolvedValue([
      {
        id: 'c1',
        type: challenge.type,
        cadence: challenge.cadence ?? ChallengeCadence.PERMANENT,
        target: challenge.target ?? 999,
        points: 10,
      },
    ]);
    mockPrisma.userChallenge.findMany.mockResolvedValue([]);
    mockPrisma.userChallenge.upsert.mockResolvedValue({});

    await service.checkAndAward('user1', challenge.type);

    const call = mockPrisma.userChallenge.upsert.mock.calls[0][0] as {
      create: { progress: number };
    };
    return call.create.progress;
  }

  beforeEach(() => {
    jest.useFakeTimers();
    jest.setSystemTime(WEEK_08);
  });
  afterEach(() => jest.useRealTimers());

  it('STUDY_PLAN_CREATED cuenta los planes del alumno', async () => {
    mockPrisma.studyPlan.count.mockResolvedValue(4);

    expect(await progressFor({ type: ChallengeType.STUDY_PLAN_CREATED })).toBe(4);
    expect(mockPrisma.studyPlan.count).toHaveBeenCalledWith({ where: { userId: 'user1' } });
  });

  it('STUDY_PLAN_CREATED con cadencia WEEKLY filtra desde el lunes', async () => {
    mockPrisma.studyPlan.count.mockResolvedValue(1);

    await progressFor({ type: ChallengeType.STUDY_PLAN_CREATED, cadence: ChallengeCadence.WEEKLY });

    expect(mockPrisma.studyPlan.count).toHaveBeenCalledWith({
      where: { userId: 'user1', createdAt: { gte: new Date('2026-02-16T00:00:00.000Z') } },
    });
  });

  it('TOPICS_STUDIED cuenta los temas de los planes del alumno', async () => {
    mockPrisma.studyPlanTopic.count.mockResolvedValue(9);

    expect(await progressFor({ type: ChallengeType.TOPICS_STUDIED })).toBe(9);
    expect(mockPrisma.studyPlanTopic.count).toHaveBeenCalledWith({
      where: { plan: { userId: 'user1' } },
    });
  });

  it('SUBJECT_VARIETY cuenta cursos de contexto distintos', async () => {
    mockPrisma.studyPlanTopic.findMany.mockResolvedValue([
      { contextCourseId: 'mates' },
      { contextCourseId: 'lengua' },
    ]);

    expect(await progressFor({ type: ChallengeType.SUBJECT_VARIETY })).toBe(2);
    expect(mockPrisma.studyPlanTopic.findMany).toHaveBeenCalledWith({
      where: { plan: { userId: 'user1' } },
      select: { contextCourseId: true },
      distinct: ['contextCourseId'],
    });
  });

  it('EXERCISES_SOLVED cuenta solo los aciertos', async () => {
    mockPrisma.exerciseAttempt.count.mockResolvedValue(12);

    expect(await progressFor({ type: ChallengeType.EXERCISES_SOLVED })).toBe(12);
    expect(mockPrisma.exerciseAttempt.count).toHaveBeenCalledWith({
      where: { userId: 'user1', verdict: 'correct' },
    });
  });

  it('HARD_EXERCISES_SOLVED filtra ademas por dificultad HARD', async () => {
    mockPrisma.exerciseAttempt.count.mockResolvedValue(3);

    await progressFor({ type: ChallengeType.HARD_EXERCISES_SOLVED });

    expect(mockPrisma.exerciseAttempt.count).toHaveBeenCalledWith({
      where: { userId: 'user1', verdict: 'correct', difficulty: 'HARD' },
    });
  });

  it('EXERCISES_CORRECT_STREAK lee la racha denormalizada', async () => {
    expect(await progressFor({ type: ChallengeType.EXERCISES_CORRECT_STREAK })).toBe(7);
  });

  it('EXAM_PERFECT cuenta los examenes con 100', async () => {
    mockPrisma.examAttempt.count.mockResolvedValue(2);

    expect(await progressFor({ type: ChallengeType.EXAM_PERFECT })).toBe(2);
    expect(mockPrisma.examAttempt.count).toHaveBeenCalledWith({
      where: { userId: 'user1', score: 100, submittedAt: { not: null } },
    });
  });

  it('EXAM_HARD_SCORE toma el maximo de los examenes de nivel HARD', async () => {
    mockPrisma.examAttempt.aggregate.mockResolvedValue({ _max: { score: 87.4 } });

    expect(await progressFor({ type: ChallengeType.EXAM_HARD_SCORE })).toBe(87);
    expect(mockPrisma.examAttempt.aggregate).toHaveBeenCalledWith({
      where: { userId: 'user1', submittedAt: { not: null }, aiExamBank: { level: 'HARD' } },
      _max: { score: true },
    });
  });

  it('TUTOR_QUESTIONS cuenta solo los mensajes del alumno', async () => {
    mockPrisma.tutorMessage.count.mockResolvedValue(15);

    expect(await progressFor({ type: ChallengeType.TUTOR_QUESTIONS })).toBe(15);
    expect(mockPrisma.tutorMessage.count).toHaveBeenCalledWith({
      where: { userId: 'user1', role: 'user' },
    });
  });

  it('STREAK_DAILY lee la racha diaria actual', async () => {
    expect(await progressFor({ type: ChallengeType.STREAK_DAILY })).toBe(1);
  });
});
```

Añadir `ChallengeCadence` al import de `@prisma/client` en la cabecera del spec.

- [ ] **Step 2: Ejecutar los tests y comprobar que fallan**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: FAIL — los tipos nuevos caen en el `default: return 0` del switch.

- [ ] **Step 3: Reescribir `calculateProgress`**

Sustituir el método completo (incluida la constante `EXERCISE_LESSON_TYPES`, que ya no se usa: borrarla junto al import de `LessonType`):

```ts
  /**
   * Calcula el progreso actual del usuario para un tipo de reto.
   * `since` llega solo en retos WEEKLY: los tipos contables filtran por
   * fecha, los de estado (máximos, rachas, variedad) lo ignoran.
   */
  private async calculateProgress(
    userId: string,
    type: ChallengeType,
    since?: Date,
  ): Promise<number> {
    switch (type) {
      // ── Plan de estudio ──
      case ChallengeType.STUDY_PLAN_CREATED:
        return this.prisma.studyPlan.count({
          where: { userId, ...(since ? { createdAt: { gte: since } } : {}) },
        });

      case ChallengeType.TOPICS_STUDIED:
        // StudyPlanTopic no tiene timestamp propio: la ventana va por el plan
        return this.prisma.studyPlanTopic.count({
          where: { plan: { userId, ...(since ? { createdAt: { gte: since } } : {}) } },
        });

      case ChallengeType.SUBJECT_VARIETY: {
        // contextCourseId cubre también los temas CUSTOM fuera de la asignatura base
        const rows = await this.prisma.studyPlanTopic.findMany({
          where: { plan: { userId } },
          select: { contextCourseId: true },
          distinct: ['contextCourseId'],
        });
        return rows.length;
      }

      case ChallengeType.THEORY_COMPLETED:
        return this.prisma.theoryModule.count({
          where: { userId, ...(since ? { createdAt: { gte: since } } : {}) },
        });

      // ── Ejercicios del plan ──
      case ChallengeType.EXERCISES_SOLVED:
        return this.prisma.exerciseAttempt.count({
          where: {
            userId,
            verdict: 'correct',
            ...(since ? { answeredAt: { gte: since } } : {}),
          },
        });

      case ChallengeType.HARD_EXERCISES_SOLVED:
        return this.prisma.exerciseAttempt.count({
          where: {
            userId,
            verdict: 'correct',
            difficulty: 'HARD',
            ...(since ? { answeredAt: { gte: since } } : {}),
          },
        });

      case ChallengeType.EXERCISES_CORRECT_STREAK: {
        const u = await this.prisma.user.findUnique({
          where: { id: userId },
          select: { currentCorrectStreak: true },
        });
        return u?.currentCorrectStreak ?? 0;
      }

      // ── Exámenes ──
      case ChallengeType.EXAM_COMPLETED:
        return this.prisma.examAttempt.count({
          where: { userId, submittedAt: since ? { gte: since } : { not: null } },
        });

      case ChallengeType.EXAM_SCORE: {
        const agg = await this.prisma.examAttempt.aggregate({
          where: { userId, submittedAt: { not: null } },
          _max: { score: true },
        });
        return Math.round(agg._max.score ?? 0);
      }

      case ChallengeType.EXAM_PERFECT:
        return this.prisma.examAttempt.count({
          where: {
            userId,
            score: 100,
            submittedAt: since ? { gte: since } : { not: null },
          },
        });

      case ChallengeType.EXAM_HARD_SCORE: {
        const agg = await this.prisma.examAttempt.aggregate({
          where: { userId, submittedAt: { not: null }, aiExamBank: { level: 'HARD' } },
          _max: { score: true },
        });
        return Math.round(agg._max.score ?? 0);
      }

      // ── Hábito ──
      case ChallengeType.TUTOR_QUESTIONS:
        return this.prisma.tutorMessage.count({
          where: { userId, role: 'user', ...(since ? { createdAt: { gte: since } } : {}) },
        });

      case ChallengeType.STREAK_DAILY: {
        const u = await this.prisma.user.findUnique({
          where: { id: userId },
          select: { currentDailyStreak: true },
        });
        return u?.currentDailyStreak ?? 0;
      }

      case ChallengeType.STREAK_WEEKLY: {
        const u = await this.prisma.user.findUnique({
          where: { id: userId },
          select: { currentStreak: true },
        });
        return u?.currentStreak ?? 0;
      }

      default:
        return 0;
    }
  }
```

- [ ] **Step 4: Ejecutar los tests y comprobar que pasan**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: PASS los 11 tests nuevos (los de `checkAndAward` con periodo se arreglan en Task 5).

- [ ] **Step 5: Verificación por mutación**

Quitar `verdict: 'correct'` del `where` de `EXERCISES_SOLVED` y ejecutar. Expected: FALLA el test de `EXERCISES_SOLVED`. Deshacer.

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/challenges/challenges.service.ts apps/api/src/challenges/challenges.service.spec.ts
git commit -m "feat(challenges): calculateProgress cubre los 14 tipos con ventana temporal"
```

---

## Task 5: `checkAndAward` con periodo y streaks implícitos

**Files:**
- Modify: `apps/api/src/challenges/challenges.service.ts`
- Test: `apps/api/src/challenges/challenges.service.spec.ts`

**Interfaces:**
- Consumes: `calculateProgress` (Task 4), `isoWeek`/`currentWeekStart` (Task 2).
- Produces:
  - `checkAndAward(userId: string, ...eventTypes: ChallengeType[]): Promise<void>` — resuelve `periodKey` por reto y añade siempre `STREAK_DAILY` y `STREAK_WEEKLY` a los tipos evaluados.
  - `bumpCorrectStreak(userId: string, correct: boolean): Promise<void>` — público, lo usa Task 7.

- [ ] **Step 1: Escribir los tests que fallan**

Añadir al `describe('checkAndAward')` existente:

```ts
it('evalua los retos de racha aunque el evento no los mencione', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 3,
    longestStreak: 3,
    lastActiveWeek: ISO_W08,
    currentDailyStreak: 3,
    longestDailyStreak: 3,
    lastActiveDay: '2026-02-16',
  });
  mockPrisma.challenge.findMany.mockResolvedValue([]);

  await service.checkAndAward('user1', ChallengeType.EXAM_COMPLETED);

  // El filtro de tipos debe incluir las dos rachas además del evento recibido
  const where = mockPrisma.challenge.findMany.mock.calls[0][0].where as {
    type: { in: ChallengeType[] };
  };
  expect(where.type.in).toEqual(
    expect.arrayContaining([
      ChallengeType.EXAM_COMPLETED,
      ChallengeType.STREAK_DAILY,
      ChallengeType.STREAK_WEEKLY,
    ]),
  );
});

it('usa periodKey ALL en retos permanentes', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 1,
    longestStreak: 1,
    lastActiveWeek: ISO_W08,
    currentDailyStreak: 1,
    longestDailyStreak: 1,
    lastActiveDay: '2026-02-16',
  });
  mockPrisma.challenge.findMany.mockResolvedValue([
    {
      id: 'c1',
      type: ChallengeType.TUTOR_QUESTIONS,
      cadence: ChallengeCadence.PERMANENT,
      target: 5,
      points: 20,
    },
  ]);
  mockPrisma.tutorMessage.count.mockResolvedValue(2);
  mockPrisma.userChallenge.findMany.mockResolvedValue([]);
  mockPrisma.userChallenge.upsert.mockResolvedValue({});

  await service.checkAndAward('user1', ChallengeType.TUTOR_QUESTIONS);

  expect(mockPrisma.userChallenge.upsert).toHaveBeenCalledWith(
    expect.objectContaining({
      where: {
        userId_challengeId_periodKey: {
          userId: 'user1',
          challengeId: 'c1',
          periodKey: 'ALL',
        },
      },
    }),
  );
});

it('usa la semana ISO como periodKey en retos semanales', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 1,
    longestStreak: 1,
    lastActiveWeek: ISO_W08,
    currentDailyStreak: 1,
    longestDailyStreak: 1,
    lastActiveDay: '2026-02-16',
  });
  mockPrisma.challenge.findMany.mockResolvedValue([
    {
      id: 'c2',
      type: ChallengeType.EXERCISES_SOLVED,
      cadence: ChallengeCadence.WEEKLY,
      target: 20,
      points: 15,
    },
  ]);
  mockPrisma.exerciseAttempt.count.mockResolvedValue(5);
  mockPrisma.userChallenge.findMany.mockResolvedValue([]);
  mockPrisma.userChallenge.upsert.mockResolvedValue({});

  await service.checkAndAward('user1', ChallengeType.EXERCISES_SOLVED);

  expect(mockPrisma.userChallenge.upsert).toHaveBeenCalledWith(
    expect.objectContaining({
      where: {
        userId_challengeId_periodKey: {
          userId: 'user1',
          challengeId: 'c2',
          periodKey: ISO_W08,
        },
      },
    }),
  );
});

it('un reto semanal completado la semana pasada vuelve a conceder puntos', async () => {
  mockPrisma.user.findUnique.mockResolvedValue({
    currentStreak: 1,
    longestStreak: 1,
    lastActiveWeek: ISO_W08,
    currentDailyStreak: 1,
    longestDailyStreak: 1,
    lastActiveDay: '2026-02-16',
  });
  mockPrisma.challenge.findMany.mockResolvedValue([
    {
      id: 'c2',
      type: ChallengeType.EXERCISES_SOLVED,
      cadence: ChallengeCadence.WEEKLY,
      target: 3,
      points: 15,
    },
  ]);
  mockPrisma.exerciseAttempt.count.mockResolvedValue(3);
  // Fila de la semana ANTERIOR, ya completada: no debe bloquear la de esta
  mockPrisma.userChallenge.findMany.mockResolvedValue([
    { challengeId: 'c2', periodKey: ISO_W07, completed: true },
  ]);
  mockPrisma.userChallenge.upsert.mockResolvedValue({});
  mockPrisma.user.update.mockResolvedValue({});

  await service.checkAndAward('user1', ChallengeType.EXERCISES_SOLVED);

  expect(mockPrisma.user.update).toHaveBeenCalledWith({
    where: { id: 'user1' },
    data: { totalPoints: { increment: 15 } },
  });
});
```

Y un `describe` nuevo para `bumpCorrectStreak`:

```ts
describe('bumpCorrectStreak', () => {
  it('incrementa la racha y el record al acertar', async () => {
    mockPrisma.user.findUnique.mockResolvedValue({
      currentCorrectStreak: 4,
      longestCorrectStreak: 4,
    });
    mockPrisma.user.update.mockResolvedValue({});

    await service.bumpCorrectStreak('user1', true);

    expect(mockPrisma.user.update).toHaveBeenCalledWith({
      where: { id: 'user1' },
      data: { currentCorrectStreak: 5, longestCorrectStreak: 5 },
    });
  });

  it('pone la racha a cero al fallar sin tocar el record', async () => {
    mockPrisma.user.update.mockResolvedValue({});

    await service.bumpCorrectStreak('user1', false);

    expect(mockPrisma.user.update).toHaveBeenCalledWith({
      where: { id: 'user1' },
      data: { currentCorrectStreak: 0 },
    });
  });
});
```

- [ ] **Step 2: Ejecutar los tests y comprobar que fallan**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: FAIL — `bumpCorrectStreak is not a function` y el upsert usa la clave doble vieja.

- [ ] **Step 3: Reescribir `checkAndAward` y añadir `bumpCorrectStreak`**

```ts
  /** Clave de periodo del reto: "ALL" si es permanente, la semana ISO si es semanal */
  private periodKeyFor(cadence: ChallengeCadence, weekKey: string): string {
    return cadence === ChallengeCadence.WEEKLY ? weekKey : 'ALL';
  }

  /**
   * Evalúa y otorga retos para el userId dados uno o varios tipos de evento.
   * Llamar con void (sin await) para no bloquear la respuesta HTTP.
   */
  async checkAndAward(userId: string, ...eventTypes: ChallengeType[]): Promise<void> {
    try {
      // 1. Actualizar rachas primero (necesarias para STREAK_DAILY / STREAK_WEEKLY)
      await this.updateStreak(userId);

      // 2. Los retos de racha se evalúan siempre, los pase o no el punto de llamada
      const types = [
        ...new Set([...eventTypes, ChallengeType.STREAK_DAILY, ChallengeType.STREAK_WEEKLY]),
      ];
      const challenges = await this.prisma.challenge.findMany({
        where: { isActive: true, type: { in: types } },
      });
      if (challenges.length === 0) return;

      const now = new Date();
      const weekKey = isoWeek(now);
      const weekStart = currentWeekStart(now);

      // 3. Progreso por (tipo, cadencia): la ventana cambia el número, así que
      //    dos retos del mismo tipo con distinta cadencia no comparten cálculo
      const progressKey = (c: { type: ChallengeType; cadence: ChallengeCadence }) =>
        `${c.type}|${c.cadence}`;
      const uniqueCombos = [...new Map(challenges.map((c) => [progressKey(c), c])).values()];
      const progressEntries = await Promise.all(
        uniqueCombos.map(
          async (c) =>
            [
              progressKey(c),
              await this.calculateProgress(
                userId,
                c.type,
                c.cadence === ChallengeCadence.WEEKLY ? weekStart : undefined,
              ),
            ] as const,
        ),
      );
      const progressByCombo = new Map(progressEntries);

      // 4. UserChallenge existentes del periodo relevante, en una sola consulta
      const existingList = await this.prisma.userChallenge.findMany({
        where: {
          userId,
          challengeId: { in: challenges.map((c) => c.id) },
          periodKey: { in: ['ALL', weekKey] },
        },
      });
      const existingMap = new Map(
        existingList.map((uc) => [`${uc.challengeId}|${uc.periodKey}`, uc]),
      );

      let pointsToAward = 0;

      // 5. Upserts en paralelo (cada uno afecta a una clave distinta)
      await Promise.all(
        challenges.map(async (challenge) => {
          const periodKey = this.periodKeyFor(challenge.cadence, weekKey);
          const existing = existingMap.get(`${challenge.id}|${periodKey}`);

          // Si ya está completado en ESTE periodo, no tocar
          if (existing?.completed) return;

          const progress = progressByCombo.get(progressKey(challenge)) ?? 0;
          const completed = progress >= challenge.target;

          await this.prisma.userChallenge.upsert({
            where: {
              userId_challengeId_periodKey: { userId, challengeId: challenge.id, periodKey },
            },
            update: {
              progress,
              ...(completed
                ? { completed: true, completedAt: new Date(), awardedPoints: challenge.points }
                : {}),
            },
            create: {
              userId,
              challengeId: challenge.id,
              periodKey,
              progress,
              completed,
              completedAt: completed ? new Date() : null,
              awardedPoints: completed ? challenge.points : 0,
            },
          });

          if (completed) pointsToAward += challenge.points;
        }),
      );

      // 6. Un único incremento de totalPoints
      if (pointsToAward > 0) {
        await this.prisma.user.update({
          where: { id: userId },
          data: { totalPoints: { increment: pointsToAward } },
        });
      }
    } catch (err) {
      this.logger.error(`Error en checkAndAward para userId=${userId}`, err);
    }
  }

  /**
   * Mueve la racha de aciertos en ejercicios. Se llama SOLO al crear un
   * ExerciseAttempt nuevo: reintentar uno ya respondido no la mueve.
   */
  async bumpCorrectStreak(userId: string, correct: boolean): Promise<void> {
    if (!correct) {
      await this.prisma.user.update({
        where: { id: userId },
        data: { currentCorrectStreak: 0 },
      });
      return;
    }
    const u = await this.prisma.user.findUnique({
      where: { id: userId },
      select: { currentCorrectStreak: true, longestCorrectStreak: true },
    });
    if (!u) return;
    const next = u.currentCorrectStreak + 1;
    await this.prisma.user.update({
      where: { id: userId },
      data: {
        currentCorrectStreak: next,
        longestCorrectStreak: Math.max(u.longestCorrectStreak, next),
      },
    });
  }
```

Añadir `ChallengeCadence` al import de `@prisma/client` y `currentWeekStart` al import de `./challenge-periods` en el service.

**Nota sobre el cambio de `!existing?.completed`:** en la versión anterior el `update` solo marcaba completado si `!existing?.completed`. Como arriba ya salimos temprano cuando `existing?.completed` es `true`, la condición es redundante y se elimina.

- [ ] **Step 4: Ejecutar los tests y comprobar que pasan**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: PASS todo el fichero.

- [ ] **Step 5: Verificación por mutación**

Quitar `ChallengeType.STREAK_DAILY, ChallengeType.STREAK_WEEKLY` del array `types` y ejecutar. Expected: FALLA "evalua los retos de racha aunque el evento no los mencione". Deshacer.

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/challenges/challenges.service.ts apps/api/src/challenges/challenges.service.spec.ts
git commit -m "feat(challenges): checkAndAward con periodo semanal y rachas siempre evaluadas"
```

---

## Task 6: `getMyProgress` y `getSummary` con cadencia y racha diaria

**Files:**
- Modify: `apps/api/src/challenges/challenges.service.ts`
- Test: `apps/api/src/challenges/challenges.service.spec.ts`

**Interfaces:**
- Consumes: `periodKeyFor`, `isoWeek` (Task 5).
- Produces:
  - `getMyProgress(userId)` → `{ meta: { totalPoints, currentStreak, longestStreak, currentDailyStreak, longestDailyStreak }, challenges: Array<Challenge & { progress, completed, completedAt, awardedPoints }> }`. Cada reto incluye su `cadence` (viene del spread de la fila).
  - `getSummary(userId)` → añade `currentDailyStreak` y `longestDailyStreak`.

- [ ] **Step 1: Escribir el test que falla**

```ts
describe('getMyProgress', () => {
  beforeEach(() => {
    jest.useFakeTimers();
    jest.setSystemTime(WEEK_08);
  });
  afterEach(() => jest.useRealTimers());

  it('empareja cada reto con la fila de SU periodo', async () => {
    mockPrisma.challenge.findMany.mockResolvedValue([
      { id: 'perm', type: ChallengeType.TUTOR_QUESTIONS, cadence: ChallengeCadence.PERMANENT, target: 10 },
      { id: 'week', type: ChallengeType.EXERCISES_SOLVED, cadence: ChallengeCadence.WEEKLY, target: 20 },
    ]);
    mockPrisma.userChallenge.findMany.mockResolvedValue([
      { challengeId: 'perm', periodKey: 'ALL', progress: 6, completed: false, completedAt: null, awardedPoints: 0 },
      { challengeId: 'week', periodKey: ISO_W07, progress: 20, completed: true, completedAt: new Date(), awardedPoints: 15 },
      { challengeId: 'week', periodKey: ISO_W08, progress: 4, completed: false, completedAt: null, awardedPoints: 0 },
    ]);
    mockPrisma.user.findUnique.mockResolvedValue({
      totalPoints: 120,
      currentStreak: 2,
      longestStreak: 5,
      currentDailyStreak: 3,
      longestDailyStreak: 8,
    });

    const result = await service.getMyProgress('user1');

    expect(result.meta.currentDailyStreak).toBe(3);
    expect(result.meta.longestDailyStreak).toBe(8);
    // El reto semanal muestra la semana EN CURSO, no la anterior ya completada
    expect(result.challenges.find((c) => c.id === 'week')).toMatchObject({
      progress: 4,
      completed: false,
    });
    expect(result.challenges.find((c) => c.id === 'perm')).toMatchObject({ progress: 6 });
  });
});
```

- [ ] **Step 2: Ejecutar el test y comprobar que falla**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: FAIL — el reto semanal coge la fila de W07 (la primera con ese `challengeId`).

- [ ] **Step 3: Reescribir `getMyProgress` y `getSummary`**

```ts
  /** Lista todos los retos activos enriquecidos con el progreso del usuario */
  async getMyProgress(userId: string) {
    const weekKey = isoWeek(new Date());

    const [challenges, userChallenges, user] = await Promise.all([
      this.prisma.challenge.findMany({ where: { isActive: true }, orderBy: { createdAt: 'asc' } }),
      this.prisma.userChallenge.findMany({
        where: { userId, periodKey: { in: ['ALL', weekKey] } },
      }),
      this.prisma.user.findUnique({
        where: { id: userId },
        select: {
          totalPoints: true,
          currentStreak: true,
          longestStreak: true,
          currentDailyStreak: true,
          longestDailyStreak: true,
        },
      }),
    ]);

    const progressMap = new Map(
      userChallenges.map((uc) => [`${uc.challengeId}|${uc.periodKey}`, uc]),
    );

    return {
      meta: {
        totalPoints: user?.totalPoints ?? 0,
        currentStreak: user?.currentStreak ?? 0,
        longestStreak: user?.longestStreak ?? 0,
        currentDailyStreak: user?.currentDailyStreak ?? 0,
        longestDailyStreak: user?.longestDailyStreak ?? 0,
      },
      challenges: challenges.map((c) => {
        const uc = progressMap.get(`${c.id}|${this.periodKeyFor(c.cadence, weekKey)}`);
        return {
          ...c,
          progress: uc?.progress ?? 0,
          completed: uc?.completed ?? false,
          completedAt: uc?.completedAt ?? null,
          awardedPoints: uc?.awardedPoints ?? 0,
        };
      }),
    };
  }
```

En `getSummary`, ampliar el `select` del usuario y el objeto devuelto:

```ts
        select: {
          totalPoints: true,
          currentStreak: true,
          longestStreak: true,
          currentDailyStreak: true,
          longestDailyStreak: true,
        },
```

```ts
    return {
      totalPoints: user?.totalPoints ?? 0,
      currentStreak: user?.currentStreak ?? 0,
      longestStreak: user?.longestStreak ?? 0,
      currentDailyStreak: user?.currentDailyStreak ?? 0,
      longestDailyStreak: user?.longestDailyStreak ?? 0,
      completedCount,
      recentBadges,
    };
```

- [ ] **Step 4: Ejecutar los tests y comprobar que pasan**

```bash
pnpm --filter @vkbacademy/api test -- challenges.service
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/challenges/challenges.service.ts apps/api/src/challenges/challenges.service.spec.ts
git commit -m "feat(challenges): getMyProgress resuelve el periodo en curso y expone la racha diaria"
```

---

## Task 7: Identidad estable de los ejercicios

**Files:**
- Modify: `packages/shared/src/types/study.types.ts`
- Modify: `apps/api/src/exercises/exercises.service.ts`
- Modify: `apps/api/src/study-plans/study-plans.service.ts`
- Test: `apps/api/src/exercises/exercises.service.spec.ts`

**Interfaces:**
- Consumes: nada de tareas previas.
- Produces:
  - `StudyExercise` gana `id: string` (shared).
  - `GeneratedExercise` gana `id: string` (API).
  - `ExercisesService.normalizeForMatch(value: string): string` — export nombrado del módulo, no método de clase.
  - `withExerciseIds(exercises: GeneratedTopicExercise[]): GeneratedTopicExercise[]` — export nombrado de `study-plans.service.ts`; rellena con `legacy-<índice>` los ejercicios sin `id`.

- [ ] **Step 1: Escribir el test que falla**

En `apps/api/src/exercises/exercises.service.spec.ts`, añadir:

```ts
import { normalizeForMatch } from './exercises.service';

describe('normalizeForMatch', () => {
  it('ignora espacios, dolares y dfrac frente a frac', () => {
    expect(normalizeForMatch('$\\dfrac{1}{2}$')).toBe(normalizeForMatch('\\frac{1}{2}'));
  });

  it('distingue respuestas realmente distintas', () => {
    expect(normalizeForMatch('12')).not.toBe(normalizeForMatch('21'));
  });
});
```

Y un test de que la generación inyecta ids, dentro del `describe` de `generateForTopics` que ya exista (o uno nuevo si no lo hay). Reutiliza el mock del `AiProviderService` que ese `describe` ya monta para devolver ejercicios; si el test se escribe en un `describe` nuevo, copiar ahí ese mismo `mockResolvedValue`:

```ts
it('inyecta un id unico en cada ejercicio generado', async () => {
  const res = await service.generateForTopics({
    courseId: 'c1',
    topics: ['Fracciones'],
    perTopic: { easy: 1, medium: 1, hard: 0 },
  });

  const ids = res.exercises.map((e) => e.id);
  expect(ids.every((id) => typeof id === 'string' && id.length > 0)).toBe(true);
  expect(new Set(ids).size).toBe(ids.length);
});
```

- [ ] **Step 2: Ejecutar el test y comprobar que falla**

```bash
pnpm --filter @vkbacademy/api test -- exercises.service
```

Expected: FAIL — `normalizeForMatch` no está exportado y los ejercicios no tienen `id`.

- [ ] **Step 3: Añadir `id` a los tipos compartidos**

En `packages/shared/src/types/study.types.ts`:

```ts
export interface StudyExercise {
  id: string; // id estable dentro de StudyPlan.exercises; base de ExerciseAttempt
  statement: string;
  type: StudyExerciseType;
  options: string[];
  solution: string;
  explanation: string;
}
```

- [ ] **Step 4: Inyectar ids y exportar `normalizeForMatch` en la API**

En `apps/api/src/exercises/exercises.service.ts`:

```ts
import { randomUUID } from 'node:crypto';
```

Añadir `id: string;` a `GeneratedExercise`:

```ts
export interface GeneratedExercise {
  id: string;
  statement: string;
  type: ExerciseType;
  options: string[];
  solution: string;
  explanation: string;
}
```

Añadir al final del fichero, fuera de la clase:

```ts
/**
 * Normaliza una respuesta para compararla con la solución: ignora espacios,
 * delimitadores LaTeX y la variante \dfrac. Vive aquí porque la corrección
 * de SINGLE/TRUE_FALSE es server-side.
 */
export function normalizeForMatch(value: string): string {
  return value.replace(/\s+/g, '').replace(/\$/g, '').replace(/\\dfrac/g, '\\frac');
}
```

Y en `generateForTopics`, justo antes del `return`, mapear los ejercicios generados para darles id:

```ts
    // Id estable por ejercicio: es la clave de ExerciseAttempt y sobrevive a
    // la regeneración del plan (los intentos viejos siguen contando).
    const withIds = exercises.map((e) => ({ ...e, id: randomUUID() }));
    return { exercises: withIds };
```

Aplicar el mismo mapeo en el método de generación del flujo un-tema si existe otro `return { exercises }` en el fichero.

- [ ] **Step 5: Añadir `withExerciseIds` en study-plans**

En `apps/api/src/study-plans/study-plans.service.ts`, fuera de la clase:

```ts
/**
 * Garantiza que todo ejercicio tenga `id`. Los planes creados antes de
 * Retos v2 lo llevan vacío: reciben un id derivado del índice, estable
 * mientras no se regenere el plan.
 */
export function withExerciseIds(exercises: GeneratedTopicExercise[]): GeneratedTopicExercise[] {
  return exercises.map((e, i) => (e.id ? e : { ...e, id: `legacy-${i}` }));
}
```

Y usarlo en `getById`, donde hoy hace el cast de `plan.exercises`:

```ts
    const exercises = Array.isArray(plan.exercises)
      ? withExerciseIds(plan.exercises as unknown as GeneratedTopicExercise[])
      : null;
```

- [ ] **Step 6: Ejecutar los tests y comprobar que pasan**

```bash
pnpm --filter @vkbacademy/api test -- exercises.service
pnpm --filter @vkbacademy/api exec tsc --noEmit
```

Expected: PASS. Si `tsc` se queja de que algún test o fixture construye un `StudyExercise` sin `id`, añadirlo en esos fixtures.

- [ ] **Step 7: Commit**

```bash
git add packages/shared/src/types/study.types.ts apps/api/src/exercises apps/api/src/study-plans/study-plans.service.ts
git commit -m "feat(study): id estable por ejercicio y correccion normalizada en servidor"
```

---

## Task 8: Endpoint de intento de ejercicio

**Files:**
- Create: `apps/api/src/study-plans/dto/submit-exercise-attempt.dto.ts`
- Modify: `apps/api/src/study-plans/study-plans.service.ts`
- Modify: `apps/api/src/study-plans/study-plans.controller.ts`
- Modify: `apps/api/src/study-plans/study-plans.module.ts`
- Test: `apps/api/src/study-plans/study-plans.service.spec.ts`

**Interfaces:**
- Consumes: `withExerciseIds`, `normalizeForMatch` (Task 7); `ChallengesService.bumpCorrectStreak` y `checkAndAward` (Task 5).
- Produces: `StudyPlansService.submitExerciseAttempt(userId: string, planId: string, exerciseId: string, dto: SubmitExerciseAttemptDto): Promise<{ verdict: EvaluationVerdict; feedback?: string; solution: string; explanation: string }>`.

- [ ] **Step 1: Escribir el DTO**

Crear `apps/api/src/study-plans/dto/submit-exercise-attempt.dto.ts`:

```ts
import { IsString, MaxLength, MinLength } from 'class-validator';

export class SubmitExerciseAttemptDto {
  @IsString()
  @MinLength(1, { message: 'La respuesta no puede estar vacía' })
  @MaxLength(2000, { message: 'La respuesta es demasiado larga' })
  answer: string;
}
```

- [ ] **Step 2: Escribir los tests que fallan**

En `apps/api/src/study-plans/study-plans.service.spec.ts`, añadir `exerciseAttempt` al mock de Prisma (`findUnique`, `create`, `update`) y un `ChallengesService` mock (`{ checkAndAward: jest.fn(), bumpCorrectStreak: jest.fn() }`) entre los providers. Después:

```ts
describe('submitExerciseAttempt', () => {
  const PLAN = {
    id: 'plan1',
    userId: 'user1',
    courseId: 'c1',
    topics: [],
    exercises: [
      {
        id: 'ex-1',
        statement: '¿Cuánto es 1/2 + 1/2?',
        type: 'SINGLE',
        options: ['1', '2'],
        solution: '1',
        explanation: 'Suma de fracciones con igual denominador.',
        topicLabel: 'Fracciones',
        difficulty: 'HARD',
      },
    ],
  };

  it('corrige en servidor y guarda el intento como correcto', async () => {
    mockPrisma.studyPlan.findUnique.mockResolvedValue(PLAN);
    mockPrisma.exerciseAttempt.findUnique.mockResolvedValue(null);
    mockPrisma.exerciseAttempt.create.mockResolvedValue({});

    const res = await service.submitExerciseAttempt('user1', 'plan1', 'ex-1', { answer: '1' });

    expect(res.verdict).toBe('correct');
    expect(mockPrisma.exerciseAttempt.create).toHaveBeenCalledWith({
      data: {
        userId: 'user1',
        studyPlanId: 'plan1',
        exerciseId: 'ex-1',
        topicLabel: 'Fracciones',
        difficulty: 'HARD',
        verdict: 'correct',
      },
    });
    expect(mockChallenges.bumpCorrectStreak).toHaveBeenCalledWith('user1', true);
  });

  it('marca incorrecto cuando la respuesta no coincide', async () => {
    mockPrisma.studyPlan.findUnique.mockResolvedValue(PLAN);
    mockPrisma.exerciseAttempt.findUnique.mockResolvedValue(null);
    mockPrisma.exerciseAttempt.create.mockResolvedValue({});

    const res = await service.submitExerciseAttempt('user1', 'plan1', 'ex-1', { answer: '2' });

    expect(res.verdict).toBe('incorrect');
    expect(mockChallenges.bumpCorrectStreak).toHaveBeenCalledWith('user1', false);
  });

  it('un reintento actualiza sin mover la racha de aciertos', async () => {
    mockPrisma.studyPlan.findUnique.mockResolvedValue(PLAN);
    mockPrisma.exerciseAttempt.findUnique.mockResolvedValue({ id: 'att1' });
    mockPrisma.exerciseAttempt.update.mockResolvedValue({});

    await service.submitExerciseAttempt('user1', 'plan1', 'ex-1', { answer: '1' });

    expect(mockPrisma.exerciseAttempt.create).not.toHaveBeenCalled();
    expect(mockPrisma.exerciseAttempt.update).toHaveBeenCalled();
    expect(mockChallenges.bumpCorrectStreak).not.toHaveBeenCalled();
  });

  it('devuelve 404 si el ejercicio no esta en el plan', async () => {
    mockPrisma.studyPlan.findUnique.mockResolvedValue(PLAN);

    await expect(
      service.submitExerciseAttempt('user1', 'plan1', 'no-existe', { answer: '1' }),
    ).rejects.toThrow(NotFoundException);
  });

  it('devuelve 403 si el plan no es del alumno', async () => {
    mockPrisma.studyPlan.findUnique.mockResolvedValue({ ...PLAN, userId: 'otro' });

    await expect(
      service.submitExerciseAttempt('user1', 'plan1', 'ex-1', { answer: '1' }),
    ).rejects.toThrow(ForbiddenException);
  });
});
```

**Nota:** el último test asume que `requireOwnedPlan` lanza `ForbiddenException`. Comprobar en `study-plans.service.ts` qué excepción lanza realmente y ajustar el test a esa, sin cambiar el comportamiento existente.

- [ ] **Step 3: Ejecutar los tests y comprobar que fallan**

```bash
pnpm --filter @vkbacademy/api test -- study-plans.service
```

Expected: FAIL — `submitExerciseAttempt is not a function`.

- [ ] **Step 4: Implementar el método**

En `study-plans.service.ts`, añadir al constructor la dependencia:

```ts
    private readonly challenges: ChallengesService,
```

e importar `ChallengesService`, `ChallengeType`, `NotFoundException`, `normalizeForMatch` y `EvaluationVerdict`.

```ts
  /**
   * Registra el intento de un ejercicio del plan. La corrección es siempre
   * server-side: el cliente manda la respuesta, nunca el veredicto — si no,
   * los puntos se farmean desde la consola del navegador.
   */
  async submitExerciseAttempt(
    userId: string,
    planId: string,
    exerciseId: string,
    dto: SubmitExerciseAttemptDto,
  ): Promise<{
    verdict: EvaluationVerdict;
    feedback?: string;
    solution: string;
    explanation: string;
  }> {
    const plan = await this.requireOwnedPlan(userId, planId);

    const exercises = withExerciseIds(
      Array.isArray(plan.exercises) ? (plan.exercises as unknown as GeneratedTopicExercise[]) : [],
    );
    const exercise = exercises.find((e) => e.id === exerciseId);
    if (!exercise) {
      throw new NotFoundException('Ejercicio no encontrado en este plan');
    }

    let verdict: EvaluationVerdict;
    let feedback: string | undefined;

    if (exercise.type === 'OPEN') {
      const evaluation = await this.exercises.evaluate({
        statement: exercise.statement,
        studentAnswer: dto.answer,
        solution: exercise.solution,
      });
      verdict = evaluation.verdict;
      feedback = evaluation.feedback;
    } else {
      verdict =
        normalizeForMatch(dto.answer) === normalizeForMatch(exercise.solution)
          ? 'correct'
          : 'incorrect';
    }

    const existing = await this.prisma.exerciseAttempt.findUnique({
      where: { userId_exerciseId: { userId, exerciseId } },
    });

    if (existing) {
      // Reintento: se guarda el último veredicto pero no mueve la racha
      await this.prisma.exerciseAttempt.update({
        where: { id: existing.id },
        data: { verdict, answeredAt: new Date() },
      });
    } else {
      await this.prisma.exerciseAttempt.create({
        data: {
          userId,
          studyPlanId: plan.id,
          exerciseId,
          topicLabel: exercise.topicLabel ?? '',
          difficulty: exercise.difficulty ?? 'MEDIUM',
          verdict,
        },
      });
      // Debe resolverse antes de checkAndAward para que la racha esté fresca
      await this.challenges.bumpCorrectStreak(userId, verdict === 'correct');
    }

    void this.challenges.checkAndAward(
      userId,
      ChallengeType.EXERCISES_SOLVED,
      ChallengeType.HARD_EXERCISES_SOLVED,
      ChallengeType.EXERCISES_CORRECT_STREAK,
    );

    return { verdict, feedback, solution: exercise.solution, explanation: exercise.explanation };
  }
```

- [ ] **Step 5: Añadir la ruta y el import del módulo**

En `study-plans.controller.ts`:

```ts
  // Los ejercicios OPEN llaman a la IA para evaluar: mismo límite que /exercises/evaluate.
  /** Registra el intento de un ejercicio del plan, corregido en servidor. */
  @Post(':id/exercises/:exerciseId/attempt')
  @Throttle({ default: { ttl: 3600000, limit: 30 } })
  submitExerciseAttempt(
    @CurrentUser() user: User,
    @Param('id') id: string,
    @Param('exerciseId') exerciseId: string,
    @Body() dto: SubmitExerciseAttemptDto,
  ) {
    return this.studyPlans.submitExerciseAttempt(user.id, id, exerciseId, dto);
  }
```

En `study-plans.module.ts`, añadir `ChallengesModule` a `imports`.

- [ ] **Step 6: Ejecutar los tests y comprobar que pasan**

```bash
pnpm --filter @vkbacademy/api test -- study-plans.service
```

Expected: PASS los 5 tests nuevos.

- [ ] **Step 7: Verificación por mutación**

Cambiar `await this.challenges.bumpCorrectStreak(...)` para que se llame también en la rama del reintento. Expected: FALLA "un reintento actualiza sin mover la racha de aciertos". Deshacer.

- [ ] **Step 8: Commit**

```bash
git add apps/api/src/study-plans
git commit -m "feat(study): endpoint de intento de ejercicio con correccion server-side"
```

---

## Task 9: Cableado de los puntos de llamada

**Files:**
- Modify: `apps/api/src/study-plans/study-plans.service.ts`
- Modify: `apps/api/src/exams/exams.service.ts:292`
- Modify: `apps/api/src/theory/theory.service.ts:142`
- Modify: `apps/api/src/progress/progress.service.ts:91`
- Modify: `apps/api/src/quizzes/quizzes.service.ts:84`
- Modify: `apps/api/src/tutor/tutor.service.ts`, `apps/api/src/tutor/tutor.module.ts`

**Interfaces:**
- Consumes: `checkAndAward` (Task 5), `ChallengesModule` exporta ya `ChallengesService`.
- Produces: nada nuevo; deja el árbol compilando tras el cambio de enum.

- [ ] **Step 1: Actualizar `study-plans.service.create()`**

Al final de `create`, justo antes del `return this.getById(userId, plan.id);`:

```ts
    // Crear un plan es actividad: cuenta para las rachas además de los retos
    void this.challenges.checkAndAward(
      userId,
      ChallengeType.STUDY_PLAN_CREATED,
      ChallengeType.TOPICS_STUDIED,
      ChallengeType.SUBJECT_VARIETY,
    );

    return this.getById(userId, plan.id);
```

- [ ] **Step 2: Actualizar `exams.service.ts`**

Sustituir el bloque de `checkAndAward` (línea ~292):

```ts
    void this.challenges.checkAndAward(
      userId,
      ChallengeType.EXAM_COMPLETED,
      ChallengeType.EXAM_SCORE,
      ChallengeType.EXAM_PERFECT,
      ChallengeType.EXAM_HARD_SCORE,
    );
```

- [ ] **Step 3: Actualizar `theory.service.ts`**

Sustituir el bloque (línea ~142):

```ts
    void this.challenges.checkAndAward(userId, ChallengeType.THEORY_COMPLETED);
```

- [ ] **Step 4: Actualizar `progress.service.ts` y `quizzes.service.ts`**

En `progress.service.ts` (línea ~91):

```ts
    // El temario clásico ya no alimenta ningún reto de ejercicio, pero sigue
    // siendo actividad: la llamada mantiene vivas las rachas diaria y semanal.
    void this.challenges.checkAndAward(userId);
```

En `quizzes.service.ts` (línea ~84):

```ts
    // Íd.: sin tipos, solo registra actividad para las rachas.
    void this.challenges.checkAndAward(userId);
```

Quitar de ambos ficheros el import de `ChallengeType` si queda sin uso.

- [ ] **Step 5: Cablear el tutor**

En `apps/api/src/tutor/tutor.module.ts`:

```ts
import { Module } from '@nestjs/common';
import { ChallengesModule } from '../challenges/challenges.module';
import { TutorController } from './tutor.controller';
import { TutorService } from './tutor.service';

@Module({
  imports: [ChallengesModule],
  controllers: [TutorController],
  providers: [TutorService],
})
export class TutorModule {}
```

En `tutor.service.ts`, inyectar `private readonly challenges: ChallengesService` en el constructor y añadir, justo después del `tutorMessage.create` del paso 2 de `streamChat` (antes de construir el system prompt, para no retrasar el streaming):

```ts
    // Preguntar al tutor cuenta como actividad y alimenta TUTOR_QUESTIONS
    void this.challenges.checkAndAward(userId, ChallengeType.TUTOR_QUESTIONS);
```

Importar `ChallengeType` de `@prisma/client` y `ChallengesService` de `../challenges/challenges.service`.

- [ ] **Step 6: Compilar y ejecutar toda la suite**

```bash
pnpm --filter @vkbacademy/api exec tsc --noEmit
pnpm --filter @vkbacademy/api test
```

Expected: `tsc` limpio y suite en verde. Si algún spec de `exams`/`theory`/`progress`/`quizzes` asertaba los tipos de evento antiguos, actualizarlo a los nuevos.

- [ ] **Step 7: Commit**

```bash
git add apps/api/src
git commit -m "feat(challenges): cablea los eventos de reto al flujo real del alumno"
```

---

## Task 10: Cadencia en el admin

**Files:**
- Modify: `apps/api/src/admin/dto/create-challenge.dto.ts`
- Modify: `apps/api/src/admin/dto/update-challenge.dto.ts`
- Modify: `apps/api/src/admin/admin-gamification.service.ts`
- Test: `apps/api/src/admin/admin-gamification.service.spec.ts` (crear si no existe)

**Interfaces:**
- Consumes: `isWeeklyCapable` de `../challenges/challenge-periods` (Task 2).
- Produces: `CreateChallengeDto.cadence?: ChallengeCadence`, `UpdateChallengeDto.cadence?: ChallengeCadence`; `createChallenge` y `updateChallenge` lanzan `BadRequestException` si la combinación tipo × cadencia es imposible.

- [ ] **Step 1: Escribir el test que falla**

Crear (o ampliar) `apps/api/src/admin/admin-gamification.service.spec.ts`:

```ts
import { BadRequestException } from '@nestjs/common';
import { Test } from '@nestjs/testing';
import { ChallengeCadence, ChallengeType } from '@prisma/client';
import { AdminGamificationService } from './admin-gamification.service';
import { PrismaService } from '../prisma/prisma.service';

describe('AdminGamificationService — cadencia', () => {
  let service: AdminGamificationService;
  const mockPrisma = {
    challenge: { create: jest.fn(), findUnique: jest.fn(), update: jest.fn() },
  };

  beforeEach(async () => {
    const moduleRef = await Test.createTestingModule({
      providers: [AdminGamificationService, { provide: PrismaService, useValue: mockPrisma }],
    }).compile();
    service = moduleRef.get(AdminGamificationService);
    jest.clearAllMocks();
  });

  it('rechaza WEEKLY en un tipo de estado', async () => {
    await expect(
      service.createChallenge({
        title: 'Racha imposible',
        description: 'No debería poder crearse',
        type: ChallengeType.STREAK_DAILY,
        cadence: ChallengeCadence.WEEKLY,
        target: 5,
        points: 10,
      }),
    ).rejects.toThrow(BadRequestException);
    expect(mockPrisma.challenge.create).not.toHaveBeenCalled();
  });

  it('acepta WEEKLY en un tipo contable', async () => {
    mockPrisma.challenge.create.mockResolvedValue({ id: 'c1' });

    await service.createChallenge({
      title: 'Semana intensa',
      description: 'Resuelve 20 ejercicios esta semana',
      type: ChallengeType.EXERCISES_SOLVED,
      cadence: ChallengeCadence.WEEKLY,
      target: 20,
      points: 15,
    });

    expect(mockPrisma.challenge.create).toHaveBeenCalled();
  });

  it('rechaza cambiar a WEEKLY un reto de tipo de estado al editarlo', async () => {
    mockPrisma.challenge.findUnique.mockResolvedValue({
      id: 'c1',
      type: ChallengeType.EXAM_SCORE,
      cadence: ChallengeCadence.PERMANENT,
    });

    await expect(
      service.updateChallenge('c1', { cadence: ChallengeCadence.WEEKLY }),
    ).rejects.toThrow(BadRequestException);
  });
});
```

- [ ] **Step 2: Ejecutar el test y comprobar que falla**

```bash
pnpm --filter @vkbacademy/api test -- admin-gamification
```

Expected: FAIL — no hay validación, `create` se llama igualmente.

- [ ] **Step 3: Añadir `cadence` a los DTOs**

En `create-challenge.dto.ts`:

```ts
import { ChallengeCadence, ChallengeType } from '@prisma/client';
```

```ts
  @IsOptional()
  @IsEnum(ChallengeCadence)
  cadence?: ChallengeCadence;
```

Lo mismo en `update-challenge.dto.ts`.

- [ ] **Step 4: Validar en el service**

En `admin-gamification.service.ts`:

```ts
import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { ChallengeCadence, ChallengeType } from '@prisma/client';
import { isWeeklyCapable } from '../challenges/challenge-periods';
```

```ts
  /**
   * Los retos de estado (máximos, rachas, variedad acumulada) no admiten
   * cadencia semanal: reiniciarlos cada lunes no significa nada.
   */
  private assertCadence(type: ChallengeType, cadence: ChallengeCadence): void {
    if (cadence === ChallengeCadence.WEEKLY && !isWeeklyCapable(type)) {
      throw new BadRequestException(
        `El tipo ${type} no admite cadencia semanal: mide un estado acumulado, no una cantidad por periodo.`,
      );
    }
  }

  async createChallenge(dto: CreateChallengeDto) {
    const cadence = dto.cadence ?? ChallengeCadence.PERMANENT;
    this.assertCadence(dto.type, cadence);
    return this.prisma.challenge.create({ data: { ...dto, cadence } });
  }

  async updateChallenge(id: string, dto: UpdateChallengeDto) {
    const challenge = await this.prisma.challenge.findUnique({ where: { id } });
    if (!challenge) throw new NotFoundException('Reto no encontrado');
    this.assertCadence(dto.type ?? challenge.type, dto.cadence ?? challenge.cadence);
    return this.prisma.challenge.update({ where: { id }, data: dto });
  }
```

- [ ] **Step 5: Ejecutar el test y comprobar que pasa**

```bash
pnpm --filter @vkbacademy/api test -- admin-gamification
```

Expected: PASS los 3 tests.

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/admin
git commit -m "feat(admin): cadencia de reto validada contra el tipo"
```

---

## Task 11: Seed con el catálogo nuevo

**Files:**
- Modify: `apps/api/prisma/seed.ts`

**Interfaces:**
- Consumes: enum de Task 1, validación de Task 10.
- Produces: `challengesData` con 20 retos cubriendo los 14 tipos, 5 de ellos `WEEKLY`.

- [ ] **Step 1: Reemplazar el bloque `challengesData`**

Sustituir el array completo (líneas ~262-416) por:

```ts
  // Retos de ejemplo — 20 retos cubriendo los 14 ChallengeType.
  // Los WEEKLY se reinician cada semana ISO y vuelven a conceder puntos,
  // así que su puntuación es deliberadamente más baja que la de los logros.
  const challengesData: Array<{
    title: string;
    description: string;
    type: ChallengeType;
    cadence?: ChallengeCadence;
    target: number;
    points: number;
    badgeIcon: string;
    badgeColor: string;
  }> = [
    // ── Arranque y amplitud ──────────────────────────────────────────────────
    { title: 'Primer plan', description: 'Crea tu primer plan de estudio.', type: ChallengeType.STUDY_PLAN_CREATED, target: 1, points: 15, badgeIcon: '🚀', badgeColor: '#10b981' },
    { title: 'Planificador', description: 'Crea 5 planes de estudio.', type: ChallengeType.STUDY_PLAN_CREATED, target: 5, points: 50, badgeIcon: '🗺️', badgeColor: '#059669' },
    { title: 'Diez temas', description: 'Estudia 10 temas distintos.', type: ChallengeType.TOPICS_STUDIED, target: 10, points: 60, badgeIcon: '📚', badgeColor: '#0ea5e9' },
    { title: 'Treinta temas', description: 'Estudia 30 temas distintos.', type: ChallengeType.TOPICS_STUDIED, target: 30, points: 150, badgeIcon: '🧠', badgeColor: '#0284c7' },
    { title: 'Todoterreno', description: 'Estudia temas de 3 asignaturas distintas.', type: ChallengeType.SUBJECT_VARIETY, target: 3, points: 70, badgeIcon: '🎒', badgeColor: '#8b5cf6' },
    { title: 'Teoría al día', description: 'Genera 15 mazos de teoría.', type: ChallengeType.THEORY_COMPLETED, target: 15, points: 80, badgeIcon: '📖', badgeColor: '#6366f1' },

    // ── Ejercicios ───────────────────────────────────────────────────────────
    { title: 'Primeros aciertos', description: 'Acierta 10 ejercicios.', type: ChallengeType.EXERCISES_SOLVED, target: 10, points: 30, badgeIcon: '✏️', badgeColor: '#22c55e' },
    { title: 'Cien dianas', description: 'Acierta 100 ejercicios.', type: ChallengeType.EXERCISES_SOLVED, target: 100, points: 200, badgeIcon: '🎯', badgeColor: '#16a34a' },
    { title: 'Sin fallar', description: 'Encadena 10 aciertos seguidos.', type: ChallengeType.EXERCISES_CORRECT_STREAK, target: 10, points: 90, badgeIcon: '⚡', badgeColor: '#f59e0b' },
    { title: 'Imparable', description: 'Encadena 25 aciertos seguidos.', type: ChallengeType.EXERCISES_CORRECT_STREAK, target: 25, points: 220, badgeIcon: '🔥', badgeColor: '#ea580c' },
    { title: 'Sin miedo', description: 'Acierta 20 ejercicios difíciles.', type: ChallengeType.HARD_EXERCISES_SOLVED, target: 20, points: 160, badgeIcon: '🧗', badgeColor: '#dc2626' },

    // ── Exámenes ─────────────────────────────────────────────────────────────
    { title: 'Primer examen', description: 'Entrega tu primer examen.', type: ChallengeType.EXAM_COMPLETED, target: 1, points: 20, badgeIcon: '📝', badgeColor: '#13aff0' },
    { title: 'Veterano', description: 'Entrega 20 exámenes.', type: ChallengeType.EXAM_COMPLETED, target: 20, points: 140, badgeIcon: '🗂️', badgeColor: '#0369a1' },
    { title: 'Notable', description: 'Consigue un 80% o más en un examen.', type: ChallengeType.EXAM_SCORE, target: 80, points: 70, badgeIcon: '⭐', badgeColor: '#ffd24d' },
    { title: 'Pleno', description: 'Consigue 3 exámenes con el 100%.', type: ChallengeType.EXAM_PERFECT, target: 3, points: 180, badgeIcon: '💯', badgeColor: '#facc15' },
    { title: 'Nivel experto', description: 'Consigue un 70% o más en un examen de nivel difícil.', type: ChallengeType.EXAM_HARD_SCORE, target: 70, points: 200, badgeIcon: '🏆', badgeColor: '#f5911e' },

    // ── Hábito (permanentes) ─────────────────────────────────────────────────
    { title: 'Pregunta sin miedo', description: 'Hazle 25 preguntas al tutor.', type: ChallengeType.TUTOR_QUESTIONS, target: 25, points: 60, badgeIcon: '💬', badgeColor: '#a855f7' },
    { title: 'Semana perfecta', description: 'Estudia 7 días seguidos.', type: ChallengeType.STREAK_DAILY, target: 7, points: 120, badgeIcon: '📅', badgeColor: '#ec4899' },
    { title: 'Mes constante', description: 'Estudia 30 días seguidos.', type: ChallengeType.STREAK_DAILY, target: 30, points: 400, badgeIcon: '🗓️', badgeColor: '#db2777' },
    { title: 'Un mes en racha', description: 'Mantén 4 semanas seguidas de actividad.', type: ChallengeType.STREAK_WEEKLY, target: 4, points: 150, badgeIcon: '🔗', badgeColor: '#14b8a6' },

    // ── Misiones semanales ───────────────────────────────────────────────────
    { title: 'Misión: 20 ejercicios', description: 'Acierta 20 ejercicios esta semana.', type: ChallengeType.EXERCISES_SOLVED, cadence: ChallengeCadence.WEEKLY, target: 20, points: 25, badgeIcon: '🗡️', badgeColor: '#22c55e' },
    { title: 'Misión: 5 difíciles', description: 'Acierta 5 ejercicios difíciles esta semana.', type: ChallengeType.HARD_EXERCISES_SOLVED, cadence: ChallengeCadence.WEEKLY, target: 5, points: 30, badgeIcon: '⛰️', badgeColor: '#dc2626' },
    { title: 'Misión: 3 temas', description: 'Estudia 3 temas nuevos esta semana.', type: ChallengeType.TOPICS_STUDIED, cadence: ChallengeCadence.WEEKLY, target: 3, points: 25, badgeIcon: '📗', badgeColor: '#0ea5e9' },
    { title: 'Misión: 2 exámenes', description: 'Entrega 2 exámenes esta semana.', type: ChallengeType.EXAM_COMPLETED, cadence: ChallengeCadence.WEEKLY, target: 2, points: 25, badgeIcon: '🧾', badgeColor: '#13aff0' },
    { title: 'Misión: pregunta al tutor', description: 'Hazle 5 preguntas al tutor esta semana.', type: ChallengeType.TUTOR_QUESTIONS, cadence: ChallengeCadence.WEEKLY, target: 5, points: 20, badgeIcon: '🙋', badgeColor: '#a855f7' },
  ];
```

Añadir `ChallengeCadence` al import de `@prisma/client` en la cabecera del seed.

- [ ] **Step 2: Ejecutar el seed contra la BD local**

```bash
pnpm --filter @vkbacademy/api prisma db seed
```

Expected: sin errores. El `deleteMany` de `userChallenge` y `challenge` que ya hay al principio del seed limpia lo anterior.

- [ ] **Step 3: Verificar en Prisma Studio**

```bash
pnpm --filter @vkbacademy/api prisma studio
```

Comprobar en la tabla `Challenge`: 25 filas, 5 con `cadence = WEEKLY`, y los 14 valores de `type` representados.

- [ ] **Step 4: Commit**

```bash
git add apps/api/prisma/seed.ts
git commit -m "chore(seed): catalogo de retos v2 con misiones semanales"
```

---

## Task 12: Tipos y admin en la web

**Files:**
- Modify: `apps/web/src/api/challenges.api.ts`
- Modify: `apps/web/src/api/admin.api.ts`
- Modify: `apps/web/src/pages/admin/AdminChallengesPage.tsx`

**Interfaces:**
- Consumes: API de Tasks 6 y 10.
- Produces: `ChallengeType` y `AdminChallengeType` con los 14 valores; `ChallengeCadence = 'PERMANENT' | 'WEEKLY'`; `WEEKLY_CAPABLE_TYPES` en el front.

- [ ] **Step 1: Actualizar `challenges.api.ts`**

```ts
export type ChallengeType =
  | 'STUDY_PLAN_CREATED'
  | 'TOPICS_STUDIED'
  | 'SUBJECT_VARIETY'
  | 'THEORY_COMPLETED'
  | 'EXERCISES_SOLVED'
  | 'HARD_EXERCISES_SOLVED'
  | 'EXERCISES_CORRECT_STREAK'
  | 'EXAM_COMPLETED'
  | 'EXAM_SCORE'
  | 'EXAM_PERFECT'
  | 'EXAM_HARD_SCORE'
  | 'TUTOR_QUESTIONS'
  | 'STREAK_DAILY'
  | 'STREAK_WEEKLY';

export type ChallengeCadence = 'PERMANENT' | 'WEEKLY';
```

Añadir `cadence: ChallengeCadence;` a `ChallengeWithProgress`, y a `ChallengesProgressResponse['meta']` y `ChallengeSummary`:

```ts
  currentDailyStreak: number;
  longestDailyStreak: number;
```

- [ ] **Step 2: Actualizar `admin.api.ts`**

```ts
import type { ChallengeCadence } from './challenges.api';

export type AdminChallengeType =
  | 'STUDY_PLAN_CREATED'
  | 'TOPICS_STUDIED'
  | 'SUBJECT_VARIETY'
  | 'THEORY_COMPLETED'
  | 'EXERCISES_SOLVED'
  | 'HARD_EXERCISES_SOLVED'
  | 'EXERCISES_CORRECT_STREAK'
  | 'EXAM_COMPLETED'
  | 'EXAM_SCORE'
  | 'EXAM_PERFECT'
  | 'EXAM_HARD_SCORE'
  | 'TUTOR_QUESTIONS'
  | 'STREAK_DAILY'
  | 'STREAK_WEEKLY';
```

Añadir `cadence: ChallengeCadence;` a la interfaz de lectura del reto (la que tipa la respuesta de `GET /admin/challenges`) y `cadence?: ChallengeCadence;` a las dos de escritura (creación y edición).

- [ ] **Step 3: Actualizar `AdminChallengesPage.tsx`**

Reemplazar `CHALLENGE_TYPE_LABELS` por:

```tsx
const CHALLENGE_TYPE_LABELS: Record<AdminChallengeType, string> = {
  STUDY_PLAN_CREATED: 'Planes de estudio creados',
  TOPICS_STUDIED: 'Temas estudiados',
  SUBJECT_VARIETY: 'Asignaturas distintas',
  THEORY_COMPLETED: 'Mazos de teoría generados',
  EXERCISES_SOLVED: 'Ejercicios acertados',
  HARD_EXERCISES_SOLVED: 'Ejercicios difíciles acertados',
  EXERCISES_CORRECT_STREAK: 'Aciertos seguidos',
  EXAM_COMPLETED: 'Exámenes entregados',
  EXAM_SCORE: 'Nota en un examen (%)',
  EXAM_PERFECT: 'Exámenes con 100%',
  EXAM_HARD_SCORE: 'Nota en examen difícil (%)',
  TUTOR_QUESTIONS: 'Preguntas al tutor',
  STREAK_DAILY: 'Racha de días',
  STREAK_WEEKLY: 'Racha de semanas',
};

// Debe coincidir con WEEKLY_CAPABLE_TYPES de apps/api/src/challenges/challenge-periods.ts
const WEEKLY_CAPABLE_TYPES: AdminChallengeType[] = [
  'STUDY_PLAN_CREATED',
  'TOPICS_STUDIED',
  'THEORY_COMPLETED',
  'EXERCISES_SOLVED',
  'HARD_EXERCISES_SOLVED',
  'EXAM_COMPLETED',
  'EXAM_PERFECT',
  'TUTOR_QUESTIONS',
];
```

Cambiar el estado inicial del formulario `type: 'EXERCISE_COMPLETED'` por `type: 'EXERCISES_SOLVED'` y añadir `cadence: 'PERMANENT'`.

Añadir el selector de cadencia junto al de tipo:

```tsx
<label>
  Cadencia
  <select
    value={form.cadence}
    onChange={(e) =>
      setForm({ ...form, cadence: e.target.value as ChallengeCadence })
    }
  >
    <option value="PERMANENT">Permanente (logro)</option>
    <option value="WEEKLY" disabled={!WEEKLY_CAPABLE_TYPES.includes(form.type)}>
      Semanal (misión)
    </option>
  </select>
</label>
```

Y al cambiar el tipo, forzar `PERMANENT` si el nuevo tipo no admite semanal:

```tsx
onChange={(e) => {
  const type = e.target.value as AdminChallengeType;
  setForm({
    ...form,
    type,
    cadence: WEEKLY_CAPABLE_TYPES.includes(type) ? form.cadence : 'PERMANENT',
  });
}}
```

En la tabla de retos, mostrar la cadencia junto al tipo:

```tsx
{CHALLENGE_TYPE_LABELS[c.type]}
{c.cadence === 'WEEKLY' && <span className="chip">Semanal</span>}
```

- [ ] **Step 4: Comprobar que compila**

```bash
pnpm --filter @vkbacademy/web exec tsc --noEmit
```

Expected: limpio.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/api apps/web/src/pages/admin/AdminChallengesPage.tsx
git commit -m "feat(admin): cadencia y catalogo de retos v2 en el panel"
```

---

## Task 13: Página de Retos del alumno

**Files:**
- Modify: `apps/web/src/pages/ChallengesPage.tsx`

**Interfaces:**
- Consumes: `ChallengeWithProgress.cadence`, `meta.currentDailyStreak`, `meta.longestDailyStreak` (Task 12).
- Produces: nada que consuman otras tareas.

- [ ] **Step 1: Partir la lista en dos bloques**

Sustituir el bloque que hoy renderiza `filtered` por dos secciones. Justo después del cálculo de `filtered`:

```tsx
  const weekly = filtered.filter((c) => c.cadence === 'WEEKLY');
  const permanent = filtered.filter((c) => c.cadence !== 'WEEKLY');
```

Y en el JSX, sustituir el `<div>` que mapea `filtered` por:

```tsx
      {weekly.length > 0 && (
        <>
          <div style={{ margin: '0 0 12px', display: 'flex', alignItems: 'center', gap: 10 }}>
            <h3 style={{ fontSize: '1rem', fontWeight: 800, color: 'var(--color-text)', margin: 0 }}>
              Misiones de la semana
            </h3>
            <span style={{ fontSize: '0.75rem', color: 'var(--color-text-muted)' }}>
              se reinician cada lunes
            </span>
          </div>
          <div style={{ display: 'flex', flexDirection: 'column' as const, gap: 14, marginBottom: 32 }}>
            {weekly.map((c, i) => (
              <ChallengeCard key={c.id} c={c} index={i} />
            ))}
          </div>
        </>
      )}

      {permanent.length > 0 && (
        <>
          <h3 style={{ fontSize: '1rem', fontWeight: 800, color: 'var(--color-text)', margin: '0 0 12px' }}>
            Logros
          </h3>
          <div style={{ display: 'flex', flexDirection: 'column' as const, gap: 14 }}>
            {permanent.map((c, i) => (
              <ChallengeCard key={c.id} c={c} index={i} />
            ))}
          </div>
        </>
      )}
```

- [ ] **Step 2: Añadir la racha diaria al hero**

Sustituir el bloque de `HeroStat`:

```tsx
          <div style={{ display: 'flex', gap: 28 }}>
            <HeroStat icon="flame" value={String(currentDailyStreak)} label="días seguidos" />
            <HeroStat icon="calendar" value={String(currentStreak)} label="semanas racha" />
            <HeroStat icon="check" value={`${completedCount}/${totalCount}`} label="retos completados" />
            <HeroStat icon="medal" value={String(longestDailyStreak)} label="mejor racha diaria" />
          </div>
```

y añadir arriba, junto a los otros:

```tsx
  const currentDailyStreak = data?.meta.currentDailyStreak ?? 0;
  const longestDailyStreak = data?.meta.longestDailyStreak ?? 0;
```

`Icon` solo pinta los nombres que tenga definidos. Comprobar en `components/ui/Icon.tsx` si existe `calendar`: si no está, usar `medal` en su lugar — añadir un icono nuevo queda fuera del alcance de esta tarea.

- [ ] **Step 3: Comprobar que compila y ver la página**

```bash
pnpm --filter @vkbacademy/web exec tsc --noEmit
pnpm dev
```

Entrar como alumno del seed en `/challenges` y comprobar: dos bloques visibles, las misiones semanales arriba, el hero con cuatro stats.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/pages/ChallengesPage.tsx
git commit -m "feat(challenges): separa misiones semanales de logros en la pagina del alumno"
```

---

## Task 14: `ExercisePractice` contra el endpoint de intento

**Files:**
- Modify: `apps/web/src/components/exercises/ExercisePractice.tsx`
- Modify: `apps/web/src/pages/StudyPlanPage.tsx` (pasar `planId`)

**Interfaces:**
- Consumes: `POST /study-plans/:id/exercises/:exerciseId/attempt` (Task 8).
- Produces: nada.

**Cuidado:** este es el punto de mayor riesgo del plan — es el flujo principal del alumno. El componente debe seguir funcionando si el endpoint falla.

- [ ] **Step 1: Aceptar `planId` como prop**

```tsx
export default function ExercisePractice({
  exercises,
  planId,
}: {
  exercises: PracticeExercise[];
  planId?: string;
}) {
```

En `StudyPlanPage.tsx`, pasar `planId={plan.id}` allí donde se renderiza `<ExercisePractice exercises={...} />`.

`planId` es opcional porque el componente también se usa desde el flujo un-tema, que no tiene plan. Sin `planId` el comportamiento es el de hoy: corrección local, sin registrar intento.

- [ ] **Step 2: Sustituir la mutación por la de intento**

```tsx
interface AttemptResult {
  verdict: Verdict;
  feedback?: string;
  solution: string;
  explanation: string;
}

  const attemptMutation = useMutation({
    mutationFn: ({ index, exerciseId, answer }: { index: number; exerciseId: string; answer: string }) =>
      api
        .post<AttemptResult>(`/study-plans/${planId}/exercises/${exerciseId}/attempt`, { answer })
        .then((r) => ({ index, data: r.data })),
    onSuccess: ({ index, data }) => {
      setEvaluations((prev) => ({
        ...prev,
        [index]: { verdict: data.verdict, feedback: data.feedback ?? '' },
      }));
      setRevealed((prev) => ({ ...prev, [index]: true }));
      setEvalErrors((prev) => {
        const next = { ...prev };
        delete next[index];
        return next;
      });
    },
    onError: (err, variables) => {
      const msg = (err as { response?: { data?: { message?: string | string[] } } } | null)
        ?.response?.data?.message;
      const text = Array.isArray(msg) ? msg.join(' · ') : msg;
      setEvalErrors((prev) => ({
        ...prev,
        [variables.index]:
          text ?? 'No se pudo registrar la respuesta. Inténtalo de nuevo en unos segundos.',
      }));
      // El alumno no se queda bloqueado: se revela la solución igualmente,
      // aunque el intento no haya llegado a registrarse.
      setRevealed((prev) => ({ ...prev, [variables.index]: true }));
    },
  });
```

- [ ] **Step 3: Enviar el intento también en los de opción**

Sustituir `chooseOption` y `evaluateOpen` por una única función que cubre los tres tipos:

```tsx
  function submitAttempt(index: number, ex: PracticeExercise) {
    // Sin plan (flujo un-tema) se mantiene la corrección local de siempre
    if (!planId) {
      setRevealed((prev) => ({ ...prev, [index]: true }));
      return;
    }
    const answer =
      ex.options.length > 0
        ? (ex.options[selected[index] ?? -1] ?? '')
        : (answers[index] ?? '').trim();
    if (!answer) return;
    attemptMutation.mutate({ index, exerciseId: ex.id, answer });
  }
```

`chooseOption` se queda solo guardando la selección; el envío ocurre al pulsar el botón de comprobar.

- [ ] **Step 4: Actualizar el paso de props a `ExerciseCard`**

Las dos props que hoy disparan la corrección (`onEvaluate` para los `OPEN`, `onToggle` para los de opción) se unifican en una sola llamada `onCheck`:

- En el tipo de props de `ExerciseCard`, sustituir `onEvaluate: () => void;` y `onToggle: () => void;` por `onCheck: () => void;`, y actualizar el destructuring de la firma.
- Dentro de `ExerciseCard`, cambiar los dos `onClick` que llamaban a `onEvaluate` / `onToggle` por `onCheck`.
- En el padre, pasar `onCheck={() => submitAttempt(i, ex)}` y borrar las props `onEvaluate` y `onToggle`.
- Borrar la función `toggleSolution`, que queda sin uso.

Actualizar `evaluatingIdx`:

```tsx
  const evaluatingIdx =
    attemptMutation.isPending && attemptMutation.variables
      ? attemptMutation.variables.index
      : null;
```

- [ ] **Step 5: Comprobar que compila y probarlo a mano**

```bash
pnpm --filter @vkbacademy/web exec tsc --noEmit
pnpm dev
```

Probar en un plan de estudio real:
1. Un ejercicio `SINGLE` acertado → se revela en verde y el reto "Primeros aciertos" avanza en `/challenges`.
2. El mismo ejercicio respondido otra vez → no vuelve a sumar.
3. Un ejercicio `OPEN` → el veredicto sigue llegando de la IA con su feedback.
4. Un ejercicio de un plan antiguo (sin `id` real, con `legacy-N`) → también registra intento.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/components/exercises/ExercisePractice.tsx apps/web/src/pages/StudyPlanPage.tsx
git commit -m "feat(study): la practica de ejercicios registra el intento en servidor"
```

---

## Task 15: Verificación final

**Files:** ninguno (solo verificación).

- [ ] **Step 1: Suite completa de la API**

```bash
pnpm --filter @vkbacademy/api test
```

Expected: todo verde. Anotar el número de tests.

- [ ] **Step 2: Type-check de la web**

```bash
pnpm --filter @vkbacademy/web exec tsc --noEmit
```

Expected: limpio.

- [ ] **Step 3: Build completo**

```bash
pnpm build
```

Expected: sin errores.

- [ ] **Step 4: Repaso manual del flujo completo**

Con la BD sembrada y `pnpm dev`, como alumno:
1. Crear un plan de estudio → `/challenges` muestra "Primer plan" completado.
2. Resolver ejercicios → "Primeros aciertos" y "Misión: 20 ejercicios" avanzan.
3. Preguntar al tutor → "Misión: pregunta al tutor" avanza.
4. Entregar un examen → "Primer examen" completado.
5. Comprobar que el bloque "Misiones de la semana" aparece separado de "Logros".

- [ ] **Step 5: Actualizar CLAUDE.md**

En la sección 6 (Modelo de datos), sustituir el bloque `enum ChallengeType` por los 14 valores nuevos y añadir `ChallengeCadence`. Añadir `ExerciseAttempt` a la lista de entidades principales.

- [ ] **Step 6: Commit final**

```bash
git add CLAUDE.md
git commit -m "docs: actualiza el modelo de retos en CLAUDE.md"
```
